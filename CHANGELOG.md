# DeepSeek Harness 桌面版 - 更新日志

## v1.13.1 (2026-09-25)

### 更新提示与通知交互优化

- **「检查更新」提到设置页最顶部（重要）**：以前它排在设置页的最后一个面板，用户要一路往下滑到底才能找到；再叠加"启动弹窗被关掉后没有入口"，等于想更新时找不到地方。现在它是设置页的**第一个面板**，进入设置即可直接看到「立即检查 / 发现新版本 / 下载并安装」。
- **侧边栏常驻「新版本 / 立即更新」入口（重要）**：发现新版本时，侧边栏底部（运行状态下方）常驻一条更新提示，把启动时的更新弹窗点「稍后再说」关掉后它依然在 —— 未下载显示「发现新版本 v1.14.0 · 立即更新」，下载中显示「正在下载 42%」，下载完成显示「已下载完成 · 点击安装」（点一下直接开始安装）。小圆点在未下载时呼吸闪烁，下载/已下载阶段停止。点击提示（未下载时）自动进入「设置」并定位到顶部的「检查更新」。
- **系统通知点击可响应**：「发现新版本」的 Windows 通知以前点击毫无反应。现在点击会把应用窗口（最小化 / 收进托盘 / 被遮挡都能恢复）拉到前台，并重新打开更新弹窗（下载中 / 已下载时定位到对应状态）；其它通知（插件安装完成、插件与 dsh 版本不兼容等）点击后也会把控制面板拉到前台。
- 新增 `update:focus` 事件通道（主进程 → 渲染进程），用于「通知点击后定位到更新入口」。侧栏入口、设置页面板、更新弹窗三者由同一份更新状态驱动，不会出现状态不一致。

### UI 更新：圆角与配色对齐 DeepSeek 官网

- **配色换成官网 design token**：强调色由微信绿 `#07c160` 换成 DeepSeek 品牌蓝 `#4d6bfe`（深色 `#6799fe`）；正文/次要文字 `#1e232c` / `#7f8287`（深色 `#fff` / `hsla(0,0%,100%,.56)`）；页面底色 `#f9f8f8` / `#0a0a0a`；面板 `#ffffff` / `#1a1a1a`；输入框与次按钮底 `#f5f5f5` / `hsla(0,0%,100%,.08)`；边框 `#e5e5e6` / `rgba(255,255,255,.10)`。
- **圆角统一并整体加大**：按官网 `--ds-radius-*` 建立 `--r-*` 体系并在其上再圆一档 —— 面板 20px、弹窗 28px、卡片·列表项·日志块 18px、按钮·输入框·侧栏项·折叠标题 14px、小按钮与图标方块 10px、徽标·芯片·进度条·开关为胶囊 100px。原来散落在 3~10px 的圆角全部纳入；并补齐了模式卡片、`<details>` 折叠说明、输入框、表格、日志块等原先还是方角的元素。
- **浮层式布局（这一档最明显的变化）**：窗口内留出 10px 边距与间距，「侧栏」和「内容区」各自成为一张 20px 圆角的卡片 —— 浅色下侧栏为白、内容区为 `#f9f8f8`（深色下分别为 `#1a1a1a` / `#0a0a0a`），面板浮在内容区底色之上并带极浅投影。层次感来自底色的细微差别，而不是重阴影。
- **细节打磨**：主题切换改为圆角分段控件（对齐 chat 端 segmented token）；侧栏激活项由左侧直角竖条改为圆角高亮块；弹窗遮罩加背景虚化；字体栈前置 `Inter`。
- 刻意**没有**改官方 WebUI（DeepSeek 自己那套界面），只改桌面端控制台，避免与官方样式打架。

### 说明 / Notes

- 本版只改交互、提示与界面样式，不涉及 dsh 运行环境、插件与更新协议。
- 新增 `RELEASE_NOTES.test.md`，配合服务端「高版本测试版」（`1.14.0-rc.1`，复用 1.13.0 安装包）验证上述交互。

## v1.13.0 (2026-09-25)

### 适配 DeepSeek Harness v0.1.7（设置存储迁移 / 插件兼容性检查 / 会话 V4 等）

> 官方发布 **dsh-v0.1.7-rc.1**（0.1.7 系列首个候选版，汇总了自 v0.1.5-rc.3 以来的变更；`latest` 仍是 0.1.5-rc.3，0.1.7 走 `next` 预发布频道）。本版让桌面端完成配套适配。官方本轮的破坏性变更中，**真正影响桌面端的只有四类**：设置存储迁移、插件版本兼容性检查、Agent 预设存储位置、patch 语义收紧；其余（`ptc-runtime` 包名统一、`workflow-ptc`、移除 E2B、默认关闭 Ralph、Node PTC 独立进程、Messages API、`spill-policy` 的 `maxInlineBytes` → `maxInlineTokens`、`agent/session-start` → `agent/created`、`readBytes`、`spawn_teammate` 等）桌面端代码零引用，不需要改动。

- **主题双向同步适配新存储（重要）**：dsh 0.1.7 起用户设置改由「当前 Profile 的插件配置」保存，落点是 `~/.dsh/profiles/<profile>/cordis.patch.yml`（条目 `- id: ui-theme` → `config.preference` / `config.fontSize`，由官方 `dsh-settings` + `dsh-config-editor` 写入）；旧的 `~/.dsh/settings.yaml` 只在启动时**导入一次**并改名为 `settings.yaml.imported`，继续写它不再生效。桌面端改为按官方优先级解析生效值（home patch → profile patch → settings.yaml → settings.yaml.imported），写入时同时更新 profile patch 与仍存在的 `settings.yaml`（兼容尚未升级/尚未导入的运行环境）；文本级精确编辑保留注释、其他条目与其他字段，写入用「临时文件 + rename」原子替换。
- **profile patch 健壮性**：官方 0.1.7 起「空文件 / 仅含注释」的 profile patch 会让 profile **启动失败**。桌面端所有写入路径（清理遗留 `- insert:` 接线行、新建 profile、主题同步）都保证文件内始终有顶层条目，需要时补回空序列 `[]`；并且只做「缺失时创建」，绝不整体覆盖官方写入的设置与预设。
- **移动端远程控制 overlay 增加校验**：给 `dsh web` 传的 `--patch` overlay（覆盖 `webserver` 行的 host/port）在写入后与启动前各校验一次；文件为空 / 缺 `webserver` 条目 / 缺 `host` 覆盖时**不再注入该参数**（服务照常启动，只失去局域网暴露能力，日志说明原因），避免官方对 patch 语义收紧后出现"服务起不来"或"手机莫名连不上"。
- **插件与 dsh 运行时的版本兼容性检查**：新增 `plugin-compat.js`，按官方语义检查插件 `peerDependencies` 中对 `@deepseek-ai/dsh` 与 `@deepseek-ai/dsh-*` 的版本范围（预发布版本参与范围匹配、`workspace:^` 视为同一运行时、未声明 DSH peer 不施加约束、无法解析的范围视为不兼容）。安装前通过 registry 预检（等价于官方「在跑 pnpm 之前失败，不下载任何内容」）；本地 tarball / 目录安装无法预检，改为安装后复核。推荐插件卡片与「已安装插件」列表均展示兼容状态。
- **精确版本例外（compatibility.json）**：与官方 `dsh plugin allow-version` 等价。不兼容的插件可在「已安装插件」点击「允许此版本」，经二次确认（明确告知可能崩溃/丢数据）后写入 profile 的 `compatibility.json`（`包名@精确版本` → 精确 DSH 运行时版本列表）；只对该精确版本生效，插件升级或 dsh 升级后自动失效。安装被兼容性检查拦下时同样提供「授予例外并继续安装」。
- **官方启动诊断识别**：识别 dsh 0.1.7 的分类信号并补一条中文可操作指引 —— 插件/组合包因版本不兼容被跳过、必需插件条目失败导致退出、`settings.yaml` 部分 section 未迁移、patch 指向的条目不存在等；Web UI 启动超时的报错也会带上已识别的官方诊断。
- **「本地修复」不再丢用户偏好**：0.1.7 起用户设置（`ui-theme`）与 Agent 预设（`preset-*`）都存放在 profile 的 `cordis.patch.yml` 里，而修复模式会删除整个 `profiles/`。现在删除前会先暂存这些条目与 `compatibility.json`，服务下次成功启动（官方已重建 profile）后自动合并回新的 patch，坏插件的接线条目依然被清掉。
- **多实例提示**：dsh 0.1.7 起支持多个 DSH 实例共存（会话被其他实例占用时官方会提示退出该实例）。桌面端在启动清理端口时，若占用者不是本 App 启动的进程，会先打印占用者（进程名 + PID）并说明「桌面端需要独占该端口才能启动自己的服务」，不再静默结束。

### 修复 / Fixed

- **修复「把降级当成更新」的版本提示（重要）**：官方正式版渠道可能**低于**用户正在运行的预发布版（例如当前 `0.1.7-rc.2` 而 `latest` 是 `0.1.5-rc.3`）。此前用「字符串不相等」判断是否有新版，于是首页弹出「发现 dsh 新版本：正式版 v0.1.5-rc.3」横幅、设置页给出「更新正式版 v0.1.5-rc.3」按钮 —— 点下去会把运行环境**降级**。现在改为 semver 严格比较（含预发布）：只有候选版本真正新于当前版本才提示；当前版本领先时首页不弹横幅，设置页把入口明确写为「切换到正式版 vX」并给出专门的回退确认文案。
- **`dsh-vault` 在 dsh 0.1.7 下的备份错位（桌面端侧兜底修正）**：该插件用 `settings.documentPath` 的父目录推断 dsh home；0.1.7 起设置迁到 profile 的 `cordis.patch.yml`，home 被算成 `~/.dsh/profiles/web` → 备份源目录（`sessions` / `storages` / `profiles`）全部不存在（robocopy 报 0x2 乱码错误），备份根也错位到 `~/.dsh/profiles/.dsh-backups`（备份管理页读不到历史备份、还可能被「本地修复」删掉）。桌面端现在会在**服务启动前**对该插件应用幂等的文本级修正（只在文档确实是 `settings.*` 时才采用其父目录，否则回落到 `DSH_HOME` / `~/.dsh`）：已安装的插件无需重装即修好，新装 / 升级路径也一并覆盖。
- **错位备份自动归位**：启动时以及「本地修复」删除 `profiles/` 之前，都会把 `~/.dsh/profiles/.dsh-backups` 里的备份搬回真正的备份根 `~/.dsh-backups`（同名项不覆盖，插件状态文件不动），避免历史备份被修复流程连带删除；归位完成后若目录里只剩已镜像到正确根的状态文件，会连空目录一起清理掉，不留尾巴。
- **启动自检（自动化总入口）**：上述「插件修正 + 错位备份归位 + 残留清理」合并为一次启动自检，在**任何 dsh 进程 spawn 之前**执行（插件修正才会在本次启动生效），全部幂等、无需用户干预，并且只在确实做了事情时输出一行 `[自检] 自动处理完成：…` 汇总，日志面板不再需要人工判断该不该动手。
- **开发期日志降噪（可选理解项）**：未打包运行（`electron .` / `npm start`）时自动静默 Electron 的 `Insecure Content-Security-Policy` 警告 —— 该警告只针对「页面没有 CSP」，而页面的 CSP 由页面所有者（官方 dsh web server）决定，桌面端硬加策略容易打坏官方客户端与 `dsh-resource://` 资源读取；正式安装包本来也不显示它。此项不影响任何实际安全设置（窗口仍是 `contextIsolation` + 无 `nodeIntegration`，外链一律交给系统浏览器）。`[url-compat]` 自检日志保持开启不变（它是 Electron 31 内核差异的判定依据，去掉会导致文件预览失效）。

- **修正「本地修复」的界面文案**：模式卡与启动失败提示此前仍写着「强力清除 ~/.dsh 全部本地数据」，与 v1.9.x 起的实际行为（只清理 `profiles/` 与坏插件引用，聊天记录 / 工作区 / 设置 / 凭据全部保留）不符，容易让用户以为会丢数据。已按实际行为改写中文与英文文案，并在模式卡上明确「只清理 profile 与坏插件引用」。

### 说明 / Notes

- 桌面端依然只做「安装 / 启动 / 桥接 / 诊断」：模型清单、插件加载策略、设置存储格式全部由 dsh 运行环境决定，桌面端不复制这些逻辑。
- 本版不改动官方任何文件；对 `cordis.patch.yml` 仅在缺失时创建，此后只做文本级精确编辑。
- 涉及官方行为的描述以 `dsh-v0.1.7-rc.1` 发布说明与官方已发布包（0.1.7-rc.2）的实际实现为准（`@deepseek-ai/dsh-settings`、`dsh-config-editor`、`dsh-client-ui-theme`、`dsh-app-boot`、`dsh-plugin-manager`）。

## v1.12.0 (2026-09-11)

### 适配 DeepSeek V4.1 Flash 新模型与新定价（配套更新）

> DeepSeek 官方发布 **V4.1 Flash**（模型名 `deepseek-flash`，1M 上下文 / 最大 384K 输出，原生多模态）并下调 Flash 档位价格，DeepSeek Harness 同步更新到 **v0.1.5**。本版让桌面端与配套插件完成适配；用户日常操作完全不变。

- **运行环境更新到支持新模型的 dsh**：极速启动的后台检查更新、设置页「运行环境（dsh）」一键更新到官方最新版后，官方 Web UI 的模型选择器即出现 `deepseek-flash`（DeepSeek-V4.1-Flash）与 `deepseek-v4-pro`。旧模型名 `deepseek-v4-flash` / `deepseek-v4-flash-vision-exp` 仍可调用（官方已将其路由到 V4.1 Flash 并按 Flash 单价计费）。
- **用量插件升级到 ≥ 1.17.0 即按新价格计费**：数据中心 / 首页展示的成本字段全部来自用量插件。插件 1.17.0 起按官方 **2026-09-10 12:00（北京时间）** 生效的新峰谷价计费——`deepseek-flash` 空闲 缓存命中 0.02 / 输入 1 / 输出 4 元（高峰为其两倍），`deepseek-v4-pro` 不变；并按每次调用的发生时刻自动选择新旧价格版本，历史记录口径不变。桌面端「插件管理 → 已安装插件 → 检查更新」可一键升级。
- **推荐插件卡片文案更新**：用量插件描述更新为「用量统计 / V4.1 峰谷价计费 / 余额查询 / 导出报表」，让新计价口径在客户端可见。
- **说明：桌面端本身不需要改模型配置**：桌面端只负责「安装 / 启动 / 桥接展示」，模型清单由 dsh 运行环境自带、用量成本由插件计算（`plugin-bridge.js` 只做纯数学聚合，不含任何本地计价），因此新模型上线只需更新运行环境与插件，无需修改桌面端代码。

### 修复 / Fixed

- **修复「一键更新成功后桌面端没反应」**（重要）：此前只有在服务正以「极速启动」模式运行时，更新完成后才会自动重启；服务未运行时就只弹一句「更新完成，重新运行服务即可使用新版本」。而此时若本次会话从未选过启动模式（典型场景：打开应用后直接进「设置 → 运行环境（dsh）」点「一键更新」），`selectedMode` 为空 —— 点「重新运行」会命中 `restartService()` 的模式守卫，**直接退回模式选择页**，表现为"提示更新成功、但桌面端点什么都没反应"（日志里只有停止记录、没有任何启动记录）。修复内容：
  - `updateLocalDsh()`：服务未运行时，更新完成后**直接以「极速启动」模式把服务启动起来**（`selectedMode='local'` + `run()`），更新结果立即可用，不再要求用户自己再点一次；服务正以快速 / 源码模式运行时维持原行为（不影响在跑的服务）。
  - `restartService()`：`selectedMode` 为空时不再直接退回模式选择页 —— 本地运行环境已就绪就自动按「极速启动」继续，只有确实没有本地环境时才回到模式选择。
  - 更新完成提示语相应调整（只有「快速 / 源码模式的服务正在运行」这一种情况才需要用户自行重新运行）。

### 优化 / Changed

- **启动就绪判定改为「真正可用」时刻**：实测 dsh 0.1.5 会先开放 `3080` 端口，之后还要再花 **3~9 秒**装配前端与插件、初始化认证，最后才打印带 token 的访问 URL —— 那才是窗口能加载成功的时刻。此前桌面端只要端口有响应就判定「已就绪」并把进度置 100%，于是用户会对着"已就绪"再空等数秒才看到 WebUI 打开。现在改为以下列时刻为准（`isUiReady()`）：
  - 已捕获 `dsh web: http://…/?token=…` 认证 URL，或
  - 根路径直接返回 2xx（老版 dsh / 已有会话 cookie）；
  - 并保留 25 秒兜底宽限：端口开放后若迟迟等不到 token（例如将来 dsh 改了输出格式），仍按"端口就绪"放行，不会永久卡住。
  - 等待期间的进度文案同步改为「正在加载 dsh 运行环境与插件（dsh 打印访问令牌前界面无法打开）」，不再让人误以为已经启动完成。
- **说明（实测数据）**：本次优化只消除"空等"，不改变 dsh 自身的启动耗时。实测（同一台机器，`--no-open --host 127.0.0.1`）：
  | 场景 | 进程启动 → 端口可连 | 端口可连 → 真正就绪(token) | 合计 |
  | --- | --- | --- | --- |
  | 全新 `DSH_HOME`（仅官方 base + web-app） | 16.9 s | 3.0 s | **19.9 s** |
  | 真实 profile（含全部已装插件） | 13.1 s | 3.9 s | **17.0 s** |
  插件几乎不占额外时间（差异来自磁盘缓存），**13~17 秒是 dsh 0.1.5 自身的启动成本**。

### 诊断能力 / Diagnostics

- **主界面（WebUI）渲染进程诊断写入日志**：同一地址在普通浏览器里正常、在桌面端窗口里异常时，此前日志完全看不到原因。现在主窗口的**控制台 warning/error**、**请求 4xx/5xx**、**请求失败（网络/WS）**、**实际导航到的 URL** 都会写入桌面端日志（`[WebUI:error] …` / `[WebUI] HTTP 404 …` / `[WebUI] 已导航到 …`），排查插件级问题可直接定位到具体插件与请求。
- **开发版新增 F12 打开/关闭开发者工具**（仅 `!app.isPackaged` 时生效，打包版不启用，避免普通用户误触）。

## v1.11.0 (2026-09-06)

### 适配新版 dsh 浏览器会话认证（大版本更新）

> dsh 从 `0.1.2-rc.1` 起对 Web UI 强制「浏览器会话认证」：每次启动生成一次性令牌并打印在 `dsh web: http://.../?token=...` 行，任何无 cookie 的浏览器访问首页都会返回 401。本版让桌面端**完整适配该认证**，用户依然只面对裸地址、全程无感。

- **自动捕获访问令牌并打开主界面**：启动 dsh 服务时自动从输出捕获带 token 的访问 URL，主窗口自动用它打开一次（换取 30 天有效 cookie），随后自动回到干净的裸地址 `http://127.0.0.1:3080`——地址栏、首页状态、托盘提示均无 token 残留
- **重启 / 停止服务自动重置令牌**：每次重启 dsh（重新运行 / 崩溃自愈 / 清除本地环境后重装）都会重新捕获当次进程的新令牌，避免使用已失效的旧令牌
- **兼容老版 dsh**：检测到服务无需认证（老版 dsh 根路径直接 200）时，仍按原方式用裸地址立即打开，行为不变
- **运行状态日志明确**：日志新增 `[认证] 已捕获 dsh web 访问令牌 / 主界面使用带 token 的 URL 打开` 等记录，便于排查

### 移动端远程控制体验升级（配合 dsh-mobile-remote v1.8.0）

- **远程访问显示裸地址**：设置页 / 数据中心展示的手机访问地址为干净裸地址（`http://192.168.x.x:3080`），手机直接打开即可
- 依赖插件 `@feiyang666/dsh-mobile-remote >= 1.8.0`（其服务端接管 `/` 自动完成认证），安装 / 升级插件后重启服务即可

### 插件数据桥兼容认证（修复）

- **插件数据桥自动携带浏览器会话 cookie**：dsh 新认证对 `/api` 前缀下的插件接口同样要求 cookie（如 `dsh-vault`），此前会返回 401「unauthorized」。本版在服务启动后自动用捕获到的令牌换取一次会话 cookie，插件桥的 `/api` 请求自动带上，数据中心 / 首页插件卡片恢复正常
- `plugin-bridge.js` 新增 `setAuthCookie()`，供主进程注入会话 cookie（纯 Node 逻辑，无 Electron 依赖）

### 其他

- 更新首页状态卡文案与设置页提示（首次访问自动完成连接授权说明）
- 与 `dsh-mobile-remote` v1.8.0（裸地址直达自动登录）配套

## v1.10.0 (2026-08-23)

### 插件数据桥接（新增）
- **首页运行状态控制台新增插件数据卡片**：服务运行期间，自动通过已安装插件暴露的 HTTP API 拉取并展示——用量插件（`dsh-usage-plugin`）的今日消耗 / 近 7 天消耗 / 余额、数据保险箱（`dsh-vault`）的备份份数与最近备份时间、移动端远程控制（`dsh-mobile-remote`）的在线设备数，每 60 秒自动刷新，服务停止时自动隐藏
- **插件保持完全独立可用**：上述能力全部来自插件自身（npm 包 + `dsh plugin` 一条命令即可独立使用，不依赖桌面端）；桌面端只做「桥接消费」，不重复实现任何插件逻辑。插件未安装 / 未运行 / 接口不可用时对应卡片自动隐藏，不影响主流程
- 新增 `plugin-bridge.js` 桥接模块（纯 Node 逻辑、无 Electron 依赖，可独立单测），已纳入打包
- 依赖：本功能依赖已安装的 dsh-usage-plugin / dsh-vault / dsh-mobile-remote 插件，未安装时仅不显示对应卡片，其余功能不受影响

### 数据中心栏目（新增）
- **侧边栏新增「数据中心」独立栏目**（与首页 / 插件管理 / 设置平级），一站式、分区块展示全部插件详细数据，比插件自带设置页与桌面端卡片更完整：
  - **用量统计**：今日 / 本周 / 本月 / 累计四周期汇总（成本 + 调用次数）、缓存命中率（含进度条）、近 30 天按天明细（调用 / 输入 / 缓存读 / 缓存写 / 输出 / 推理 token / 成本）、按模型分布、按服务商分布、最近 50 条调用记录明细
  - **余额与凭据**：列出全部余额服务商（DeepSeek / SiliconFlow / DigitalOcean / AMD），逐个显示凭据配置状态（已配置 / 未配置 / 失败）、余额数值、余额明细（充值 / 赠送等）、查询时间、错误原因
  - **备份管理**：备份份数、最近备份时间、备份根目录、完整备份历史（每份时间 / 会话数 / 是否含用量数据 / 大小），以及「立即备份」按钮（调用 `dsh-vault` 执行手动备份）
  - **远程设备**：在线设备数、累计设备数、累计心跳、局域网访问地址、外网隧道状态（域名 / URL / 端口 / frpc / PID / 启动时间 / 错误）、密码门禁状态、在线设备列表（设备名 / 系统 / 浏览器 / 屏幕 / IP / 在线状态 / 心跳数 / 最近活跃）、服务运行时信息（dsh 版本 / Node / 运行时长 / CPU / 内存）
- 每块数据均来自插件 HTTP API（`/usage/api`、`/api/dsh-vault`、`/__dsh_remote/status` 与 `/auth-status`）；备份大小由桌面端对本地备份目录做只读统计（不修改任何插件数据）
- **SSE 实时推送，无需手动刷新**：服务运行期间，桌面端长连接订阅三个插件新增的 SSE 事件流（`/usage/api/events`、`/api/dsh-vault/events`、`/__dsh_remote/events`），插件在数据变化**瞬间**推送（新用量记录 / 备份完成 / 设备心跳 / 隧道开关），桌面端收到即静默刷新对应数据——毫秒级实时、数据不变时不打扰；断线自动重连。另有后台指标对比（20s）作为兜底，覆盖服务停止等边界场景；「立即刷新」按钮保留
- 页面支持「刷新」按钮；服务未运行时显示提示；插件未安装时对应区块显示「未安装」说明（该插件仍可完全独立使用）

## v1.9.5 (2026-08-22)

### 修复本地修复 / 快速启动失败（修复）
- **`pnpm dlx` 不再传 `--ignore-scripts` 参数**：pnpm v11 的 `dlx` 命令不接受该参数（会直接报 `Unknown option: 'ignore-scripts'`），导致「本地修复」清理坏插件后无法启动服务、「快速启动」也启动失败。已移除命令行参数，改用环境变量 `npm_config_ignore_scripts` 设置（pnpm 兼容 npm 的 `npm_config_*` 环境变量，跳过 koffi 源码编译的效果一致），本地修复 / 快速启动恢复正常
- **「本地修复 / 自动修复」新增对 `~/.dsh/.credentials.yaml` 的检测与修复**：新版 dsh 要求该文件为「凭据引用 → 非空字符串」的严格扁平映射，旧版写入的 `version`/`schema` 元数据（值为数字/布尔）或 `entries`/`providers` 嵌套结构都会导致服务启动即崩溃（`credentials-local: the value for "version" in ... must be a string`）。该文件位于 `~/.dsh/` 下、不在 `profiles/` 内，此前「本地修复」清理不到，导致修复后反复失败。本版在清理时自动备份并修复该文件（按行移除 version/schema 元数据保留凭据；嵌套结构无法安全保留时移除文件、备份留存），修复后重启即可恢复

## v1.9.4 (2026-08-21)

### 安装/更新性能大幅提升 —— 全面切换到 pnpm（重大优化）
- **极速启动安装/更新改用 pnpm**：dsh 官方包拆成 150+ 个相互依赖的子包，npm（10/11）解析整棵依赖树时 placeDep 阶段 CPU 满转、十几分钟装不完（表现为"卡死 20 分钟"）。改用 pnpm 后实测 **1 分多钟装完 454 个包**，安装/更新速度提升 10 倍以上
- **快速启动 / 本地修复改用 `pnpm dlx`**：等价于原 npx，但依赖树解析快 10 倍以上，且能正确放置 cordis 插件的 peerDependencies
- **自动补齐 pnpm**：检测到未安装 pnpm 时自动 `npm install -g pnpm`（失败自动切换镜像重试）
- **修复装到旧版本的问题**：pnpm 解析 `@latest` 标签时可能装到旧版（如 0.1.0-rc.8，其 CLI 不支持 `--patch` 导致启动失败），改为先用 `npm view` 查询 dist-tags 拿到精确版本号再安装，确保装到官方最新版
- **修复启动参数顺序**：`--patch` 是 dsh launcher 自身参数，必须紧跟 `web`、放在所有 app 参数（`--no-open`/`--host`/`--port`）之前，否则被透传给 app 层报 `unknown option '--patch'`（移动端远程控制失效）
- **安装前清理残留**：安装/更新前清理残留的旧 dsh 进程与旧依赖锁定，避免文件占用导致 npm/pnpm 反复 EBUSY 重试、或装到旧版

### 镜像切换优化（优化）
- **官方源改为最后兜底**：镜像池中官方源（registry.npmjs.org）标记为 fallback，只在所有国内镜像都失败后才使用
- **某镜像缺包自动换其他国内镜像**：某个国内镜像 404 缺包/网络异常时，自动切换到 npmmirror / 腾讯云 / 华为云 / 淘宝 等其他国内镜像继续重试，不再"网易失败直接跳官方源"
- **安装/启动流程允许多试几个镜像**：pnpm 安装、快速启动等流程从"只切 1 次镜像"改为"把候选镜像都试一遍"

### 设置页状态显示修复（修复）
- **修复离开首页再进设置页底部误显示"未运行"**：侧栏运行状态是动态状态，语言切换（i18n）不再把它覆盖为固定文案"未运行"，并在渲染后按真实服务状态恢复（运行中→绿色运行中，其余→未运行）

### 清除本地运行环境修复（修复）
- **Windows 删除 pnpm 目录不再卡死**：pnpm 的 node_modules 含大量 junction 符号链接，`fs.promises.rm` 删除会反复重试卡死（表现为"清除中"转圈无结果）；改用 `cmd /c rmdir /s /q` 正确删除 junction，速度快、不阻塞界面，并带 60 秒超时兜底
- 清除完成后同步侧栏运行状态（服务已停止→未运行），不残留"运行中"

### 日志查看优化（优化）
- **首页运行状态新增「查看日志」按钮**：服务运行中可直接进入设置页日志区并自动滚动定位，不再需要自己找
- **设置页日志区高度提升**：`max-height` 从固定 280px 提高到 55vh，小屏幕也能查看更多日志
- **进度页日志不再被挤压**：进度页整页可滚动、日志面板不被首次安装提示卡挤压（首次安装也能看到日志）
- **首次安装自动展开日志面板**：首次装依赖时日志默认收起、误以为"看不到日志"，现在自动展开，直接看到下载/解压进度

### 进度页可退出（新增）
- 安装/更新/清除进行中，进度页新增 **「取消操作」按钮**：确认后可停止服务并退出应用，不再卡在进度页 100% 无法退出

### 推荐插件新增「移动端远程控制」（2026-08-22 补充）
- **插件管理页新增官方推荐插件「移动端远程控制」（`@feiyang666/dsh-mobile-remote`）**：手机扫码即可通过局域网 / 外网远程操控电脑上的 DeepSeek Harness，支持远程访问密码门禁、外网隧道真实状态监测、设备与 DSH 运行状态实时展示（CPU / 内存 / 会话 / 插件 / 模型）
- **插件市场同步更新**：`dsh-mobile-remote` 仓库在插件市场中置顶并标注「官方」，支持一键安装

## v1.9.3 (2026-08-17)

### 进度显示系统重构（优化）
- **进度单调递增**：进度条强制只增不减，百分比与阶段文字不再脱节（修复"正在安装依赖却显示 60%"、"某些模式到不了 100%"的问题）
- **所有模式必达 100%**：服务就绪后统一置 100%，源码/极速/快速/修复模式均能完整走到结束
- **等待期进度衔接**：启动等待从当前已到达百分比继续推进（不固定从 60 起步），不再出现跳变
- **安装阶段"慢进"**：大文件下载（首次装依赖数百 MB）期间进度每 2 秒缓慢走动，不再像"卡死"
- **首次安装醒目提示**：极速启动首次装依赖 / Node.js 首次安装 / 一键更新时，进度页显示醒目的「首次安装运行环境，通常需 3~10 分钟，请耐心等待」提示卡

### 极速启动二次提速（优化）
- **环境定位缓存**：Node/npm 路径同一会话内缓存，省去每次 `which` 子进程调用
- **本地就绪跳过 npm**：本地环境已装好时不再查找 npm、不再走安装检查，直接启动
- **就绪轮询 800ms → 300ms**：服务就绪后界面更快打开

### 清除本地运行环境（新增）
- 设置页「运行环境（dsh）」新增 **本地运行环境位置展示与清除**：直接显示安装目录完整路径（如 `%APPDATA%\dsh-desktop\dsh-local`），一键清除（停止服务 → 删除目录），清除后下次选「极速启动」会重新联网安装

### 退出确认优化（优化）
- WebUI 窗口关闭时改用 Electron 原生对话框（响应快、零延迟），提供两个选择：**退出 Web 界面**（关闭窗口保留后台服务，可随时在引导台重新打开）/ **退出 APP**（清理服务后完全退出）

### 命令行日志优化（优化）
- 日志面板新增 **「隐藏依赖下载进度」** 开关（默认开启）：`npm http fetch ... (cache revalidated)` 类刷屏日志不再显示，便于查看关键信息
- 新增 **「暂停自动滚动」** 开关：暂停自动滚到底部，便于向上回溯查看报错

### 运行状态秒数实时（修复）
- 首页运行状态「已运行 Xm Ys」由每 5 秒刷新改为**每秒实时更新**，秒数连续跳动

### 界面修复（修复）
- **修复启动完成后切换到设置/插件管理再回首页卡在 100% 的问题**：点击首页导航时重新向主进程查询真实运行状态，不再因前端阶段残留而误判为"启动中"

## v1.9.1 (2026-08-17)

### 极速启动模式（重大新增，取代「快速启动」）
- **「快速启动」合并为「极速启动」**：原「快速启动」走 `npm exec`（npx），dsh 官方包拆成 150+ 个子包，每次启动都要逐个向 registry 解析依赖树、校验并解压，即使缓存命中也要 100~200 秒。现在把「安装」与「启动」彻底分离——新增 **「极速启动」** 作为首页唯一的快速入口（原「快速启动」卡片移除，内部实现保留），启动后二次秒级完成
- **本地固定目录安装**：首次联网执行 `npm install @deepseek-ai/dsh --prefix %APPDATA%\dsh-desktop\dsh-local --ignore-scripts`，把 dsh 及全部 150+ 依赖完整解压到本地固定目录，形成一棵已就绪的依赖树；之后每次启动直接用 Node 运行本地入口 `lib/bin.js` 启动 `dsh web`——**不再经过 npm exec、不解析 latest、不发任何 HTTP 请求、不解压依赖**，Node 的 `require` 直接命中本地 node_modules，几秒内服务就绪，且完全离线可用
- **自动检查更新 + 一键更新**：不追求"每次启动都是最新版"，而是**后台静默检查**——启动后约 8 秒执行 `npm view @deepseek-ai/dsh version` 查询官方最新版（避开首次加载抢带宽；断网时静默跳过），发现新版时首页运行状态栏显示「一键更新」横幅，点击自动「停止服务 → 重装本地环境到最新版 → 自动重启」，更新全程走国内镜像、失败自动切换。**更新能力完整保留**，官方发布新版后用户点一次即可升级
- **本地环境已就绪时跳过镜像测速**：`run()` 中镜像并发测速仅在极速启动且本地环境未就绪时才执行——本地已装好时直接启动，完全离线场景无需网络探测

### 启动崩溃自动修复（重大修复）
- **原生模块崩溃识别**：识别 Windows 原生崩溃码（0xC0000005 内存访问违规、0xC0000409 栈缓冲溢出、0xC00000FD 栈溢出、0xC0000135 DLL 缺失等），崩溃时自动清理损坏的 npx 缓存并重启服务一次，仍失败给出针对性提示（删除本地运行环境重装 / 换源码安装）
- **npx 缓存损坏自动修复**：检测到「文件缺失 / main 入口缺失」类缓存损坏时，自动清理 `_npx` 缓存重新下载；若重下仍损坏，自动升级为 `npm cache verify` 深度校验后再清理重下（预算 2 次，防无限重启）
- **用户插件缺失自动摘除**：识别 `Cannot find package 'xxx'`（插件被移除但配置残留引用）并自动摘除失效引用后重新启动

### 查看日志（新增）
- 设置页新增 **「查看日志」** 区域：应用运行日志（启动、环境检测、插件安装/卸载、更新等）统一写入用户数据目录下的 `logs/dsh-desktop-YYYY-MM-DD.log` 文件，按天分文件保存
- 支持 **刷新** 查看最近 2000 行日志、**打开日志文件**（系统默认文本编辑器）、**打开日志目录**（文件管理器），便于排查问题后直接反馈日志内容
- 日志写入失败静默忽略，不影响任何主流程

### 用量插件包名变更（兼容升级）
- 用量与消耗插件推荐包名由 `@feiyang666/deepseekharnessdesktop` 更名为 **`@feiyang666/dsh-usage-plugin`**（旧包名已停止维护，仅用于兼容识别旧安装，避免「装了旧包还显示未装」）；别名映射双向兼容

### 界面修复（修复）
- **修复启动过程中离开首页再回来无法选择启动模式的问题**：首页导航现在按主进程当前阶段恢复界面——启动中回到进度页、运行中/已停止回到控制台，不再残留不可点击的模式卡片
- **修复进度页阶段标签显示函数源码**（如 `() => t('stageStart')`）：正确渲染为翻译文案

## v1.9.0 (2026-08-16)

> **重要提示（更新前必读）**
> - **低于 v1.9.0 的旧版本用户，请先卸载旧版，再重新安装本版本**：旧版在线更新存在"安装冲突"问题（见下方修复说明），请不要在旧版内使用在线更新直接升级，以免再次出现冲突导致只能卸载重装
> - **你的数据不会丢失**：所有数据（对话记录、插件、配置等）都保存在系统用户目录 `~/.dsh` 下，与程序安装目录完全分离。卸载重装不会删除这些数据，重新打开应用后数据原样保留，可继续使用

### 修复在线更新安装冲突（重大修复）
- **修复更新安装时无法覆盖正在运行的应用**：此前收到后台更新并下载完安装包后，启动安装程序时旧版应用仍在前台运行，exe 文件被占用，安装器无法覆盖导致安装冲突、只能卸载重装；现在启动安装程序后会自动退出当前应用（先停止 dsh 服务释放文件占用），并给安装器传入 `--updated` 参数，安装完成后自动打开新版应用

### 界面中英文双语切换（新增）
- 侧边栏底部新增**语言切换按钮**，一键在「简体中文 ↔ English」之间切换，偏好持久化保存、重启保持
- **全界面中英文适配**：首页（启动模式卡片 / 运行状态）、插件管理、插件市场、设置、进度页、更新弹窗等所有文案均已双语化，切换后立即全局生效

### 侧边栏增强（新增）
- 侧边栏底部新增 **「GitHub 项目」入口**，点击用系统浏览器打开项目仓库，便于反馈与参与
- 侧边栏底部新增**主题快捷切换按钮**，一键在深色 / 浅色间循环切换（图标与文字随状态变化）
- 加宽侧边栏，应用名「DeepSeek Harness 桌面版」不再被省略号截断

### Logo 与图标适配（修复）
- **窗口标题栏 / 任务栏 / Alt-Tab 图标**：提供黑白双套图标，浅色模式用黑 logo、深色模式用白 logo，两种模式下都清晰可见
- **页面 Logo（侧边栏 / 进度页 / 设置页 / 更新弹窗）**：统一白底黑 logo，深浅色主题下协调一致
- **托盘图标**：默认黑色模式

### 检测插件更新（新增）
- 插件管理页「已安装插件」区域新增 **「检查更新」按钮**，一键检测所有已安装插件是否有新版本
- 逐个查询 npm registry 最新版本并与当前版本对比，结果实时展示：可更新的插件显示「可更新 vX.Y.Z」标记与「更新」按钮，已是最新的显示「已是最新」标记
- 旧包名安装的插件自动识别并提示「旧包名，可迁移」，一键更新到新包名

### 插件卸载修复（修复）
- **修复「推荐插件卸载假成功」**：旧包名插件（如旧名数据保险箱）在推荐卡片点「卸载」时，此前只卸载新包名导致实际没删掉、却提示成功；现已别名感知，真正清理新旧包名实体目录与 pnpm 虚拟目录
- **旧包名插件识别 + 迁移**：检测到旧包名安装的插件时，提供「一键更新」（直接迁移到新包名）与「卸载重装」两种选择

### 数据保险箱插件改名
- 数据保险箱插件包名由 `@feiyang666/deepseekharnessdesktop-vault` 统一更名为 `@feiyang666/dsh-vault`，已安装旧包名的插件自动兼容识别，插件管理 / 首页 / 文档同步更新

### 插件卸载提速
- 此前卸载走 `npm uninstall`，在 pnpm 布局 + 清单与锁文件不同步时，会重建依赖树并访问默认源，导致「卸载很慢 / 像卡住 / 残留目录删不干净」；现改为纯本地文件操作，卸载秒级完成；目录被占用时以 `npm uninstall --offline` 离线兜底，不再依赖网络

### 外观全面适配
- **侧边菜单栏浅色适配**：修复浅色模式下菜单栏仍为黑色的问题，背景 / 文字 / 高亮 / 边框 / hover 全部随主题切换
- **滚动条适配深浅色**：深色主题深色滑块、浅色主题浅色滑块，不再刺眼
- **更新日志 Markdown 渲染**：设置页 / 检查更新 / 更新弹窗中的日志以富文本展示（标题 / 列表 / 代码 / 粗体 / 链接），内容经 HTML 转义，杜绝注入风险；日志中的链接改为系统浏览器打开

### 界面主题三档 + 多端同步
- 主题由「深色 / 浅色」扩展为「深色 / 浅色 / 跟随系统」三档
- **与官方 WebUI 多端同步**：控制面板或官方 UI 任意一端切换主题，另一端自动跟随；主题偏好持久化到官方 `~/.dsh/settings.yaml`，不修改任何官方代码，官方更新后依然兼容
- `system` 档跟随 Windows 系统深浅色实时变化，标题栏 / 系统弹窗 / 右键菜单统一联动

## v1.7.0 (2026-08-16)

- **修复快速/修复模式安装极慢（500 秒装不上）**：npx 启动此前未指定 npm 镜像源，导致退回默认源 npmjs.org，在国内网络下载 `@deepseek-ai/dsh` 卡到超时；现已为 npx / 版本查询显式指定国内镜像（默认 npmmirror）、启动前先测速选最快源、镜像失败自动切换重试，安装时长对齐脚本 start-web.bat 的 1 分多钟
- **环境自动补齐（官方正版）**：未检测到 Node.js / git 时，不再只给官网链接让引导断掉，而是自动下载官方安装包静默安装（Node.js LTS MSI 每用户安装、Git for Windows 每用户安装），安装完成后自动定位 node.exe / git.exe 继续流程，修复「快速启动 / 本地修复 装不了」与「引导死循环」问题
- **侧栏常驻**：启动 / 进度页也始终显示左侧导航，不再全屏覆盖隐藏侧栏
- **托盘打开主界面**：直接恢复并聚焦已存在窗口，不再强制 reload，避免打开的不是用户初始进入的页面
- **退出 App 修复**：统一退出流程并加兜底（子进程清理卡住也保证 5 秒内强制退出），修复「停止运行有效但退出 App 无作用」

## v1.6.0 (2026-08-15)

- **插件市场**：新增「插件市场」页面，扫描 GitHub 上带 `dsh-plugin` 话题的公开仓库，展示插件名称 / 作者 / 描述 / star / 语言等信息，支持搜索与分页，识别到 npm 包名后可一键安装（走与「自定义安装」相同的镜像切换安装流程）
- **界面整体重构**：引入左侧导航栏（首页 / 插件管理 / 插件市场 / 设置），替换原先靠顶部按钮跳转的结构，信息密度更高、空间利用更合理
- **去 AI 味**：收敛配色与视觉 —— 去掉发光/渐变/大阴影/多色强调，改用微信式克制的单一强调色与扁平紧凑风格，圆角、间距、字号整体收紧
- **插件管理页重构**：自定义安装 / 推荐插件 / 已安装插件改为上下贯通的全宽面板布局，告别旧的两列堆叠
- 侧栏常驻显示服务运行状态（运行中 / 未运行）与版本号
- 新增 `plugin-market.js`（纯 Node 插件市场扫描模块，可独立测试），并纳入打包文件列表
- **插件兼容性自动修补**：`@feiyang666/dsh-vault` 注册 `backup_vault` 工具时把 `required: true` 误写在 `boolean` 字段上（违反 JSON Schema 规范），导致启动时打印 `backup_vault 工具注册失败（不影响备份/恢复）` 警告。安装 / 升级该插件后会自动把 `required` 挪到 schema 顶层，消除此报警
- **macOS 打包支持**：新增 `npm run dist:mac` / `pack:mac`（dmg + zip，未签名构建），新增 mac 打包图标 `assets/icon-1024.png`
- macOS 运行兼容：进程清理改用 pgrep/lsof、托盘单击恢复主界面、支持 Homebrew 的 npm/pnpm 路径探测
- **GitHub Actions macOS 云打包**：新增 `.github/workflows/build-mac.yml`，Windows 上推送代码 / 打 `v*` 标签即自动在 macOS runner 上产出 Intel + Apple Silicon 双架构 dmg/zip（无需自备 Mac）

## v1.5.0 (2026-08-14)

- **界面主题切换**：设置页新增「外观 → 界面主题」，支持**深色 / 浅色**一键切换，选择后立即生效并持久化保存
- 浅色主题下全面适配：背景 / 卡片 / 按钮 / 输入框 / 命令行日志面板 / 弹窗等全部配色随主题切换，避免刺眼白底与低对比度
- 窗口背景色跟随主题，切换与启动时不再闪色；主题在应用启动时即时应用（预加载脚本同步传入）

## v1.4.2 (2026-08-14)

- 1.支持在桌面版中安装插件
- 2.桌面版支持更新系统
- 3.桌面版支持主题。
- 4.优化了 UI 的体验。
- 5.**dsh 运行时版本管理**：快速启动（npm exec）每次启动自动解析并更新到官方 registry 最新版，官方发布新版后下次启动自动生效；registry 不可达时自动回退到 npx 缓存版本，避免断网无法启动
- 6.首页运行状态显示当前 dsh 版本；设置页新增「运行环境（dsh）」卡片，可一键查看「当前版本 vs 最新版本」

## v1.4.0 (2026-08-14)

- **开发者选项模式**：设置页新增「开启开发者选项模式」开关（持久化保存，对下次启动生效）
- 开启后选择「快速启动」时，将「服务端后端」与「浏览器端热更 watcher（pnpm run dev:web）」**分离为两个进程**运行：修改 `dsh.client` 插件源码后自动重建 bundle、浏览器免刷新热更，便于前端开发（需先完成一次「源码完整安装」）
- 「源码完整安装」在开启开发者选项时，安装完成后自动附带启动热更 watcher
- 首页运行状态显示「开发者模式」标识；停止 / 重新运行 / 修复 / 退出会同时管理两个进程

## v1.3.0 (2026-08-14)

- **首页（控制台）重构**：引导页升级为常驻首页 —— 选择启动模式后，服务在后台运行，WebUI 在独立新窗口打开；首页显示「正在运行中」状态（运行地址 / 已运行时长），可随时**停止运行**或**重新运行**
- **插件管理独立页面**：首页新增「安装插件」入口，进入独立插件管理页；除推荐插件（@feiyang666/deepseekharnessdesktop）外，支持**自定义填写 npm 包名或安装命令**一键安装，并展示全部已安装插件（可逐个卸载）；安装 / 卸载完成后提供**「立即重启服务」**按钮一键生效
- **命令行日志空间加大**：日志面板更宽、日志区更高（最多占窗口 44% 高度），展开后页面可滚动，底部不再被裁切
- 修复停止 / 重启 / 二次启动时误报「启动失败」的问题（端口释放竞态 + 进程退出误判）
- 托盘菜单支持「停止运行 / 重新运行」；服务异常退出时首页自动回到错误提示界面

## v1.2.0 (2026-08-14)

- 新增插件管理：设置页可一键安装 / 卸载「用量与消耗插件」（@feiyang666/deepseekharnessdesktop），无需手动安装 pnpm 或编辑配置文件
- 安装逻辑与官方 `dsh plugin add` 等价：npm 装入 profile + 自动注册 `dsh.profile.bundles`，重启服务后自动加载「用量与消耗」「剩余余额查询」两个面板
- 卸载时自动清理 1.0.x 手工接线遗留的 cordis.patch.yml 引用

## v1.1.0 (2026-08-14)

- 新增在线更新功能：设置页检查更新、一键下载安装、SHA256 校验、系统通知
- 新增独立设置页面：关于 / 通知开关 / 自动检查更新
- 引导页移除倒计时自动进入，三种启动模式由用户自行选择

## v1.0.0 (2026-08-14)

- 初始版本
- 自动检测环境并拉起 dsh web 服务
- 内嵌官方 Web UI，支持系统托盘与干净退出
