# DeepSeek Harness 桌面版 - 更新日志

## v1.9.4 (2026-08-21)

### 安装/更新性能大幅提升 —— 全面切换到 pnpm（重大优化）
- **极速启动安装/更新改用 pnpm**：dsh 官方包拆成 150+ 个相互依赖的子包，npm（10/11）解析整棵依赖树时 placeDep 阶段 CPU 满转、十几分钟装不完（表现为"卡死 20 分钟"）。改用 pnpm 后实测 **1 分多钟装完 454 个包**，安装/更新速度提升 10 倍以上
- **快速启动 / 本地修复改用 `pnpm dlx`**：等价于原 npx，但依赖树解析快 10 倍以上，且能正确放置 cordis 插件的 peerDependencies
- **自动补齐 pnpm**：检测到未安装 pnpm 时自动 `npm install -g pnpm`（失败自动切换镜像重试）
- **修复装到旧版本的问题**：pnpm 解析 `@latest` 标签时可能装到旧版（如 0.1.0-rc.8，其 CLI 不支持 `--patch` 导致启动失败），改为先用 `npm view` 查询 dist-tags 拿到精确版本号再安装，确保装到官方最新版
- **修复启动参数顺序**：`--patch` 是 dsh launcher 自身参数，必须紧跟 `web`、放在所有 app 参数（`--no-open`/`--host`/`--port`）之前，否则被透传给 app 层报 `unknown option '--patch'`（移动端远程控制失效）
- **安装前清理残留**：安装/更新前清理残留的旧 dsh 进程与旧依赖锁定，避免文件占用导致 npm/pnpm 反复 EBUSY 重试、或装到旧版

### 镜像切换优化（优化）
- **官方源改为最后兜底**：镜像池中官方源（registry.npmjs.org）标记为 fallback，只在所有国内镜像都失败后才使用
- **某镜像缺包自动换其他国内镜像**：某个国内镜像 404 缺包/网络异常时，自动切换到 npmmirror / 腾讯云 / 华为云 / 淘宝 等其他国内镜像继续重试，不再"网易失败直接跳官方源"
- **安装/启动流程允许多试几个镜像**：pnpm 安装、快速启动等流程从"只切 1 次镜像"改为"把候选镜像都试一遍"

### 设置页状态显示修复（修复）
- **修复离开首页再进设置页底部误显示"未运行"**：侧栏运行状态是动态状态，语言切换（i18n）不再把它覆盖为固定文案"未运行"，并在渲染后按真实服务状态恢复（运行中→绿色运行中，其余→未运行）

### 清除本地运行环境修复（修复）
- **Windows 删除 pnpm 目录不再卡死**：pnpm 的 node_modules 含大量 junction 符号链接，`fs.promises.rm` 删除会反复重试卡死（表现为"清除中"转圈无结果）；改用 `cmd /c rmdir /s /q` 正确删除 junction，速度快、不阻塞界面，并带 60 秒超时兜底
- 清除完成后同步侧栏运行状态（服务已停止→未运行），不残留"运行中"

### 日志查看优化（优化）
- **首页运行状态新增「查看日志」按钮**：服务运行中可直接进入设置页日志区并自动滚动定位，不再需要自己找
- **设置页日志区高度提升**：`max-height` 从固定 280px 提高到 55vh，小屏幕也能查看更多日志
- **进度页日志不再被挤压**：进度页整页可滚动、日志面板不被首次安装提示卡挤压（首次安装也能看到日志）
- **首次安装自动展开日志面板**：首次装依赖时日志默认收起、误以为"看不到日志"，现在自动展开，直接看到下载/解压进度

### 进度页可退出（新增）
- 安装/更新/清除进行中，进度页新增 **「取消操作」按钮**：确认后可停止服务并退出应用，不再卡在进度页 100% 无法退出

## v1.9.3 (2026-08-17)

### 进度显示系统重构（优化）
- **进度单调递增**：进度条强制只增不减，百分比与阶段文字不再脱节（修复"正在安装依赖却显示 60%"、"某些模式到不了 100%"的问题）
- **所有模式必达 100%**：服务就绪后统一置 100%，源码/极速/快速/修复模式均能完整走到结束
- **等待期进度衔接**：启动等待从当前已到达百分比继续推进（不固定从 60 起步），不再出现跳变
- **安装阶段"慢进"**：大文件下载（首次装依赖数百 MB）期间进度每 2 秒缓慢走动，不再像"卡死"
- **首次安装醒目提示**：极速启动首次装依赖 / Node.js 首次安装 / 一键更新时，进度页显示醒目的「⏳ 首次安装运行环境，通常需 3~10 分钟，请耐心等待」提示卡

### 极速启动二次提速（优化）
- **环境定位缓存**：Node/npm 路径同一会话内缓存，省去每次 `which` 子进程调用
- **本地就绪跳过 npm**：本地环境已装好时不再查找 npm、不再走安装检查，直接启动
- **就绪轮询 800ms → 300ms**：服务就绪后界面更快打开

### 清除本地运行环境（新增）
- 设置页「运行环境（dsh）」新增 **本地运行环境位置展示与清除**：直接显示安装目录完整路径（如 `%APPDATA%\dsh-desktop\dsh-local`），一键清除（停止服务 → 删除目录），清除后下次选「极速启动」会重新联网安装

### 退出确认优化（优化）
- WebUI 窗口关闭时改用 Electron 原生对话框（响应快、零延迟），提供两个选择：**退出 Web 界面**（关闭窗口保留后台服务，可随时在引导台重新打开）/ **退出 APP**（清理服务后完全退出）

### 命令行日志优化（优化）
- 日志面板新增 **「隐藏依赖下载进度」** 开关（默认开启）：`npm http fetch ... (cache revalidated)` 类刷屏日志不再显示，便于查看关键信息
- 新增 **「暂停自动滚动」** 开关：暂停自动滚到底部，便于向上回溯查看报错

### 运行状态秒数实时（修复）
- 首页运行状态「已运行 Xm Ys」由每 5 秒刷新改为**每秒实时更新**，秒数连续跳动

### 界面修复（修复）
- **修复启动完成后切换到设置/插件管理再回首页卡在 100% 的问题**：点击首页导航时重新向主进程查询真实运行状态，不再因前端阶段残留而误判为"启动中"

## v1.9.1 (2026-08-17)

### 极速启动模式（重大新增，取代「快速启动」）
- **「快速启动」合并为「极速启动」**：原「快速启动」走 `npm exec`（npx），dsh 官方包拆成 150+ 个子包，每次启动都要逐个向 registry 解析依赖树、校验并解压，即使缓存命中也要 100~200 秒。现在把「安装」与「启动」彻底分离——新增 **「极速启动」** 作为首页唯一的快速入口（原「快速启动」卡片移除，内部实现保留），启动后二次秒级完成
- **本地固定目录安装**：首次联网执行 `npm install @deepseek-ai/dsh --prefix %APPDATA%\dsh-desktop\dsh-local --ignore-scripts`，把 dsh 及全部 150+ 依赖完整解压到本地固定目录，形成一棵已就绪的依赖树；之后每次启动直接用 Node 运行本地入口 `lib/bin.js` 启动 `dsh web`——**不再经过 npm exec、不解析 latest、不发任何 HTTP 请求、不解压依赖**，Node 的 `require` 直接命中本地 node_modules，几秒内服务就绪，且完全离线可用
- **自动检查更新 + 一键更新**：不追求"每次启动都是最新版"，而是**后台静默检查**——启动后约 8 秒执行 `npm view @deepseek-ai/dsh version` 查询官方最新版（避开首次加载抢带宽；断网时静默跳过），发现新版时首页运行状态栏显示「一键更新」横幅，点击自动「停止服务 → 重装本地环境到最新版 → 自动重启」，更新全程走国内镜像、失败自动切换。**更新能力完整保留**，官方发布新版后用户点一次即可升级
- **本地环境已就绪时跳过镜像测速**：`run()` 中镜像并发测速仅在极速启动且本地环境未就绪时才执行——本地已装好时直接启动，完全离线场景无需网络探测

### 启动崩溃自动修复（重大修复）
- **原生模块崩溃识别**：识别 Windows 原生崩溃码（0xC0000005 内存访问违规、0xC0000409 栈缓冲溢出、0xC00000FD 栈溢出、0xC0000135 DLL 缺失等），崩溃时自动清理损坏的 npx 缓存并重启服务一次，仍失败给出针对性提示（删除本地运行环境重装 / 换源码安装）
- **npx 缓存损坏自动修复**：检测到「文件缺失 / main 入口缺失」类缓存损坏时，自动清理 `_npx` 缓存重新下载；若重下仍损坏，自动升级为 `npm cache verify` 深度校验后再清理重下（预算 2 次，防无限重启）
- **用户插件缺失自动摘除**：识别 `Cannot find package 'xxx'`（插件被移除但配置残留引用）并自动摘除失效引用后重新启动

### 查看日志（新增）
- 设置页新增 **「查看日志」** 区域：应用运行日志（启动、环境检测、插件安装/卸载、更新等）统一写入用户数据目录下的 `logs/dsh-desktop-YYYY-MM-DD.log` 文件，按天分文件保存
- 支持 **刷新** 查看最近 2000 行日志、**打开日志文件**（系统默认文本编辑器）、**打开日志目录**（文件管理器），便于排查问题后直接反馈日志内容
- 日志写入失败静默忽略，不影响任何主流程

### 用量插件包名变更（兼容升级）
- 用量与消耗插件推荐包名由 `@feiyang666/deepseekharnessdesktop` 更名为 **`@feiyang666/dsh-usage-plugin`**（旧包名已停止维护，仅用于兼容识别旧安装，避免「装了旧包还显示未装」）；别名映射双向兼容

### 界面修复（修复）
- **修复启动过程中离开首页再回来无法选择启动模式的问题**：首页导航现在按主进程当前阶段恢复界面——启动中回到进度页、运行中/已停止回到控制台，不再残留不可点击的模式卡片
- **修复进度页阶段标签显示函数源码**（如 `() => t('stageStart')`）：正确渲染为翻译文案

## v1.9.0 (2026-08-16)

> **重要提示（更新前必读）**
> - **低于 v1.9.0 的旧版本用户，请先卸载旧版，再重新安装本版本**：旧版在线更新存在"安装冲突"问题（见下方修复说明），请不要在旧版内使用在线更新直接升级，以免再次出现冲突导致只能卸载重装
> - **你的数据不会丢失**：所有数据（对话记录、插件、配置等）都保存在系统用户目录 `~/.dsh` 下，与程序安装目录完全分离。卸载重装不会删除这些数据，重新打开应用后数据原样保留，可继续使用

### 修复在线更新安装冲突（重大修复）
- **修复更新安装时无法覆盖正在运行的应用**：此前收到后台更新并下载完安装包后，启动安装程序时旧版应用仍在前台运行，exe 文件被占用，安装器无法覆盖导致安装冲突、只能卸载重装；现在启动安装程序后会自动退出当前应用（先停止 dsh 服务释放文件占用），并给安装器传入 `--updated` 参数，安装完成后自动打开新版应用

### 界面中英文双语切换（新增）
- 侧边栏底部新增**语言切换按钮**，一键在「简体中文 ↔ English」之间切换，偏好持久化保存、重启保持
- **全界面中英文适配**：首页（启动模式卡片 / 运行状态）、插件管理、插件市场、设置、进度页、更新弹窗等所有文案均已双语化，切换后立即全局生效

### 侧边栏增强（新增）
- 侧边栏底部新增 **「GitHub 项目」入口**，点击用系统浏览器打开项目仓库，便于反馈与参与
- 侧边栏底部新增**主题快捷切换按钮**，一键在深色 / 浅色间循环切换（图标与文字随状态变化）
- 加宽侧边栏，应用名「DeepSeek Harness 桌面版」不再被省略号截断

### Logo 与图标适配（修复）
- **窗口标题栏 / 任务栏 / Alt-Tab 图标**：提供黑白双套图标，浅色模式用黑 logo、深色模式用白 logo，两种模式下都清晰可见
- **页面 Logo（侧边栏 / 进度页 / 设置页 / 更新弹窗）**：统一白底黑 logo，深浅色主题下协调一致
- **托盘图标**：默认黑色模式

### 检测插件更新（新增）
- 插件管理页「已安装插件」区域新增 **「检查更新」按钮**，一键检测所有已安装插件是否有新版本
- 逐个查询 npm registry 最新版本并与当前版本对比，结果实时展示：可更新的插件显示「可更新 vX.Y.Z」标记与「更新」按钮，已是最新的显示「已是最新」标记
- 旧包名安装的插件自动识别并提示「旧包名，可迁移」，一键更新到新包名

### 插件卸载修复（修复）
- **修复「推荐插件卸载假成功」**：旧包名插件（如旧名数据保险箱）在推荐卡片点「卸载」时，此前只卸载新包名导致实际没删掉、却提示成功；现已别名感知，真正清理新旧包名实体目录与 pnpm 虚拟目录
- **旧包名插件识别 + 迁移**：检测到旧包名安装的插件时，提供「一键更新」（直接迁移到新包名）与「卸载重装」两种选择

### 数据保险箱插件改名
- 数据保险箱插件包名由 `@feiyang666/deepseekharnessdesktop-vault` 统一更名为 `@feiyang666/dsh-vault`，已安装旧包名的插件自动兼容识别，插件管理 / 首页 / 文档同步更新

### 插件卸载提速
- 此前卸载走 `npm uninstall`，在 pnpm 布局 + 清单与锁文件不同步时，会重建依赖树并访问默认源，导致「卸载很慢 / 像卡住 / 残留目录删不干净」；现改为纯本地文件操作，卸载秒级完成；目录被占用时以 `npm uninstall --offline` 离线兜底，不再依赖网络

### 外观全面适配
- **侧边菜单栏浅色适配**：修复浅色模式下菜单栏仍为黑色的问题，背景 / 文字 / 高亮 / 边框 / hover 全部随主题切换
- **滚动条适配深浅色**：深色主题深色滑块、浅色主题浅色滑块，不再刺眼
- **更新日志 Markdown 渲染**：设置页 / 检查更新 / 更新弹窗中的日志以富文本展示（标题 / 列表 / 代码 / 粗体 / 链接），内容经 HTML 转义，杜绝注入风险；日志中的链接改为系统浏览器打开

### 界面主题三档 + 多端同步
- 主题由「深色 / 浅色」扩展为「深色 / 浅色 / 跟随系统」三档
- **与官方 WebUI 多端同步**：控制面板或官方 UI 任意一端切换主题，另一端自动跟随；主题偏好持久化到官方 `~/.dsh/settings.yaml`，不修改任何官方代码，官方更新后依然兼容
- `system` 档跟随 Windows 系统深浅色实时变化，标题栏 / 系统弹窗 / 右键菜单统一联动

## v1.7.0 (2026-08-16)

- **修复快速/修复模式安装极慢（500 秒装不上）**：npx 启动此前未指定 npm 镜像源，导致退回默认源 npmjs.org，在国内网络下载 `@deepseek-ai/dsh` 卡到超时；现已为 npx / 版本查询显式指定国内镜像（默认 npmmirror）、启动前先测速选最快源、镜像失败自动切换重试，安装时长对齐脚本 start-web.bat 的 1 分多钟
- **环境自动补齐（官方正版）**：未检测到 Node.js / git 时，不再只给官网链接让引导断掉，而是自动下载官方安装包静默安装（Node.js LTS MSI 每用户安装、Git for Windows 每用户安装），安装完成后自动定位 node.exe / git.exe 继续流程，修复「快速启动 / 本地修复 装不了」与「引导死循环」问题
- **侧栏常驻**：启动 / 进度页也始终显示左侧导航，不再全屏覆盖隐藏侧栏
- **托盘打开主界面**：直接恢复并聚焦已存在窗口，不再强制 reload，避免打开的不是用户初始进入的页面
- **退出 App 修复**：统一退出流程并加兜底（子进程清理卡住也保证 5 秒内强制退出），修复「停止运行有效但退出 App 无作用」

## v1.6.0 (2026-08-15)

- **插件市场**：新增「插件市场」页面，扫描 GitHub 上带 `dsh-plugin` 话题的公开仓库，展示插件名称 / 作者 / 描述 / star / 语言等信息，支持搜索与分页，识别到 npm 包名后可一键安装（走与「自定义安装」相同的镜像切换安装流程）
- **界面整体重构**：引入左侧导航栏（首页 / 插件管理 / 插件市场 / 设置），替换原先靠顶部按钮跳转的结构，信息密度更高、空间利用更合理
- **去 AI 味**：收敛配色与视觉 —— 去掉发光/渐变/大阴影/多色强调，改用微信式克制的单一强调色与扁平紧凑风格，圆角、间距、字号整体收紧
- **插件管理页重构**：自定义安装 / 推荐插件 / 已安装插件改为上下贯通的全宽面板布局，告别旧的两列堆叠
- 侧栏常驻显示服务运行状态（运行中 / 未运行）与版本号
- 新增 `plugin-market.js`（纯 Node 插件市场扫描模块，可独立测试），并纳入打包文件列表
- **插件兼容性自动修补**：`@feiyang666/dsh-vault` 注册 `backup_vault` 工具时把 `required: true` 误写在 `boolean` 字段上（违反 JSON Schema 规范），导致启动时打印 `backup_vault 工具注册失败（不影响备份/恢复）` 警告。安装 / 升级该插件后会自动把 `required` 挪到 schema 顶层，消除此报警
- **macOS 打包支持**：新增 `npm run dist:mac` / `pack:mac`（dmg + zip，未签名构建），新增 mac 打包图标 `assets/icon-1024.png`
- macOS 运行兼容：进程清理改用 pgrep/lsof、托盘单击恢复主界面、支持 Homebrew 的 npm/pnpm 路径探测
- **GitHub Actions macOS 云打包**：新增 `.github/workflows/build-mac.yml`，Windows 上推送代码 / 打 `v*` 标签即自动在 macOS runner 上产出 Intel + Apple Silicon 双架构 dmg/zip（无需自备 Mac）

## v1.5.0 (2026-08-14)

- **界面主题切换**：设置页新增「外观 → 界面主题」，支持**深色 / 浅色**一键切换，选择后立即生效并持久化保存
- 浅色主题下全面适配：背景 / 卡片 / 按钮 / 输入框 / 命令行日志面板 / 弹窗等全部配色随主题切换，避免刺眼白底与低对比度
- 窗口背景色跟随主题，切换与启动时不再闪色；主题在应用启动时即时应用（预加载脚本同步传入）

## v1.4.2 (2026-08-14)

- 1.支持在桌面版中安装插件
- 2.桌面版支持更新系统
- 3.桌面版支持主题。
- 4.优化了 UI 的体验。
- 5.**dsh 运行时版本管理**：快速启动（npm exec）每次启动自动解析并更新到官方 registry 最新版，官方发布新版后下次启动自动生效；registry 不可达时自动回退到 npx 缓存版本，避免断网无法启动
- 6.首页运行状态显示当前 dsh 版本；设置页新增「运行环境（dsh）」卡片，可一键查看「当前版本 vs 最新版本」

## v1.4.0 (2026-08-14)

- **开发者选项模式**：设置页新增「开启开发者选项模式」开关（持久化保存，对下次启动生效）
- 开启后选择「快速启动」时，将「服务端后端」与「浏览器端热更 watcher（pnpm run dev:web）」**分离为两个进程**运行：修改 `dsh.client` 插件源码后自动重建 bundle、浏览器免刷新热更，便于前端开发（需先完成一次「源码完整安装」）
- 「源码完整安装」在开启开发者选项时，安装完成后自动附带启动热更 watcher
- 首页运行状态显示「开发者模式」标识；停止 / 重新运行 / 修复 / 退出会同时管理两个进程

## v1.3.0 (2026-08-14)

- **首页（控制台）重构**：引导页升级为常驻首页 —— 选择启动模式后，服务在后台运行，WebUI 在独立新窗口打开；首页显示「正在运行中」状态（运行地址 / 已运行时长），可随时**停止运行**或**重新运行**
- **插件管理独立页面**：首页新增「安装插件」入口，进入独立插件管理页；除推荐插件（@feiyang666/deepseekharnessdesktop）外，支持**自定义填写 npm 包名或安装命令**一键安装，并展示全部已安装插件（可逐个卸载）；安装 / 卸载完成后提供**「立即重启服务」**按钮一键生效
- **命令行日志空间加大**：日志面板更宽、日志区更高（最多占窗口 44% 高度），展开后页面可滚动，底部不再被裁切
- 修复停止 / 重启 / 二次启动时误报「启动失败」的问题（端口释放竞态 + 进程退出误判）
- 托盘菜单支持「停止运行 / 重新运行」；服务异常退出时首页自动回到错误提示界面

## v1.2.0 (2026-08-14)

- 新增插件管理：设置页可一键安装 / 卸载「用量与消耗插件」（@feiyang666/deepseekharnessdesktop），无需手动安装 pnpm 或编辑配置文件
- 安装逻辑与官方 `dsh plugin add` 等价：npm 装入 profile + 自动注册 `dsh.profile.bundles`，重启服务后自动加载「用量与消耗」「剩余余额查询」两个面板
- 卸载时自动清理 1.0.x 手工接线遗留的 cordis.patch.yml 引用

## v1.1.0 (2026-08-14)

- 新增在线更新功能：设置页检查更新、一键下载安装、SHA256 校验、系统通知
- 新增独立设置页面：关于 / 通知开关 / 自动检查更新
- 引导页移除倒计时自动进入，三种启动模式由用户自行选择

## v1.0.0 (2026-08-14)

- 初始版本
- 自动检测环境并拉起 dsh web 服务
- 内嵌官方 Web UI，支持系统托盘与干净退出
