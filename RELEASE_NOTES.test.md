# 【测试版】更新提示与通知交互 测试版本（1.14.0-rc.1）

> 这是**仅供测试**的版本，用来验证桌面端的「更新提示常驻入口」与「通知点击响应」。
>
> ⚠️ 本次服务端复用的是 v1.13.0 的安装包，**请不要真的安装它**——安装后程序版本仍然是 1.13.0。

## 测试要点

**一、关闭弹窗后的更新提示**

1. 应用启动后，应弹出「发现新版本 v1.14.0-rc.1」弹窗；
2. 点弹窗里的「稍后再说」把它关掉；
3. 此时左侧边栏底部应常驻显示「发现新版本 / 立即更新」入口（不再是只能在设置页最底部才能找到）；
4. 点击该入口，应自动进入「设置」并滚动定位到「检查更新」区域。

**二、通知点击响应**

1. 让应用处于后台或最小化，触发一次更新检查（或重启应用）；
2. Windows 弹出「发现新版本」系统通知；
3. 点击该通知，应把应用窗口拉到前台，并打开更新入口（弹窗或更新区域），而不是毫无反应。

## 说明

- 本版本不包含任何功能改动，只是给更新流程造一个"更高版本"，用于测试交互。
- 测试完成后可在管理平台把该版本停用（`isActive=false`）或删除。
