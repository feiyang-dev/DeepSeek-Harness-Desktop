# DeepSeek Harness 桌面版 v1.13.0

**DeepSeek Harness 官方 Web UI 的 Windows 桌面客户端** —— 自动检测环境、安装依赖、拉起服务，并支持在线检查更新，开箱即用。

## 安装方式

下载 **`DeepSeek Harness 桌面版-Setup-1.13.0.exe`**（NSIS 安装包），双击安装即可。

- 未安装 Node.js / git 时，客户端会自动下载官方安装包静默补齐

## 本版更新（v1.13.0）—— 适配 DeepSeek Harness v0.1.7

> 官方发布 **dsh-v0.1.7-rc.1**（0.1.7 系列首个候选版，汇总自 v0.1.5-rc.3 的变更）。这一轮官方改了不少东西，其中**只有四类会真正影响桌面端**：设置存储迁移、插件版本兼容性检查、Agent 预设存储位置、patch 语义收紧。其余破坏性变更（`ptc-runtime` 包名统一、工作流执行器 `workflow-ptc`、移除 E2B、默认关闭 Ralph、Node PTC 独立进程、官方适配器仅用 Messages API、`spill-policy` 的 `maxInlineBytes` → `maxInlineTokens`、`agent/session-start` → `agent/created`、工作区读取统一 `readBytes`、Team 统一 `spawn_teammate` 等）桌面端没有任何引用，无需改动。

### 主题与设置（与官方 WebUI 的双向同步已适配新存储）

- dsh 0.1.7 起，用户设置改由**当前 Profile 的插件配置**保存：`~/.dsh/profiles/<profile>/cordis.patch.yml` 中的 `- id: ui-theme` 条目（`config.preference` 主题、`config.fontSize` 字号）；旧的 `~/.dsh/settings.yaml` 只在启动时**导入一次**，随后改名为 `settings.yaml.imported`
- 桌面端按官方优先级读取生效值（home patch → profile patch → settings.yaml → `.imported`），写入时同时更新 profile patch 与仍存在的 `settings.yaml`；编辑是文本级的，**不会动注释、其他条目或其他字段**，并采用「临时文件 + rename」原子替换
- 因此：在桌面端「设置 → 外观」切换主题，官方 WebUI 立刻跟随；在 WebUI 里切换，桌面端控制面板也会反向同步（两种 dsh 版本下都成立）

### 插件版本兼容性（新）

- 官方在**安装与启动**时都会检查插件 `peerDependencies` 中对 `@deepseek-ai/dsh` / `@deepseek-ai/dsh-*` 的版本范围；不兼容的插件会被**拒绝加载**（组合包被跳过，或插件行被置为禁用）
- 桌面端现在也会做同样的检查：安装前通过 registry 预检（与官方一致，不兼容时在下载任何内容之前就取消），本地 tarball / 目录安装在安装后复核；推荐插件卡片与「已安装插件」列表都会显示兼容状态
- 遇到不兼容的插件，可在「已安装插件」点击 **「允许此版本」** 授予**精确版本例外**（等价于官方 `dsh plugin allow-version`，写入 profile 的 `compatibility.json`）。例外只对该精确版本 + 当前运行时生效，插件升级或 dsh 升级后自动失效；授予前会明确提示可能导致崩溃或数据丢失

### 修复模式与诊断

- **「本地修复」不再丢用户偏好**：0.1.7 起用户设置与 Agent 预设都存在 profile 里。修复删除 `profiles/` 前会先暂存 `ui-theme` / `preset-*` 条目与版本例外，服务下次成功启动后自动合并回去
- **启动诊断识别**：识别官方 0.1.7 的分类信号（插件不兼容被跳过、必需插件条目失败、旧 `settings.yaml` 部分 section 未迁移、patch 条目不存在），在日志面板补一条中文可操作指引；启动超时的报错也会带上已识别的诊断
- **移动端远程控制更稳**：给 `dsh web` 的 `--patch` overlay 增加写入后 + 启动前双重校验，非法时不再注入该参数（服务照常启动，只失去局域网暴露并说明原因）
- **多实例提醒**：0.1.7 起官方支持多个 DSH 实例共存。若端口被本 App 之外的进程占用，桌面端会先打印占用者（进程名 + PID）并说明原因，再结束它；你也可以让桌面端换端口：`npm start -- --port <端口>`

### 修复与自动化

- **修复「把降级当成更新」的版本提示**：官方正式版渠道可能**低于**你正在运行的预发布版（例如当前 `0.1.7-rc.2` 而 `latest` 是 `0.1.5-rc.3`）。此前用「字符串不相等」判断是否有新版，首页会弹出「发现 dsh 新版本：正式版 v0.1.5-rc.3」横幅、设置页给出对应按钮 —— 点下去会把运行环境**降级**。现在改为 semver 严格比较（含预发布）：只有候选版本真正更新时才提示；当前版本领先时首页不弹横幅，设置页把入口明确写成「切换到正式版 vX」并给出专门的回退确认。
- **修复 dsh-vault 在 0.1.7 下备份写错位置**：该插件用 `settings.documentPath` 的父目录推断 dsh home，0.1.7 起该路径变成 profile 的 `cordis.patch.yml`，于是备份源目录（`sessions` / `storages` / `profiles`）全部找不到（robocopy 报 0x2），备份根也错位到 `~/.dsh/profiles/.dsh-backups`（备份管理页读不到历史备份，还可能被「本地修复」删掉）。桌面端现在会在**服务启动前**对已安装的该插件应用幂等的文本级修正（只在文档确实是 `settings.*` 时才采用其父目录，否则回落到 `DSH_HOME` / `~/.dsh`），已装插件无需重装。
- **启动自检（全自动，无需干预）**：上述插件修正、错位备份归位（搬回 `~/.dsh-backups`，同名不覆盖）、残留目录清理合并为一次启动自检，只在确实做了事时输出一行 `[自检] 自动处理完成：…`；「本地修复」删除 `profiles/` 之前还会再兜一次，确保备份不会被连带删除。
- **开发期日志降噪**：未打包运行（`npm start`）时自动静默 Electron 的 `Insecure Content-Security-Policy` 警告（页面 CSP 属于页面所有者官方 web server 的职责，桌面端硬加策略会打坏官方客户端与 `dsh-resource://` 资源读取；正式安装包本来也不显示该警告）。此项不改变任何实际安全设置。

### 安全提示

- dsh 0.1.7 的 Web 侧边栏新增**终端**，且它以**系统用户权限**运行，**不受 Agent 沙箱模式限制**。桌面端内嵌的官方 Web UI 因此等于提供了系统级 shell，请勿把服务端口暴露到不可信网络（移动端远程控制请务必设置访问密码）

## 从 v1.10.0 升级上来的用户（本版同时包含未单独发布的 v1.11.0 / v1.12.0）

- **v1.11.0 —— 适配官方新版浏览器会话认证**：dsh `0.1.2-rc.1+` 对 Web UI 强制浏览器会话认证。桌面端自动捕获 dsh 启动时打印的访问令牌并用它打开主界面（换取 30 天 cookie 后回到干净的裸地址），无需手动处理任何 `?token=` 链接；移动端远程访问配合插件 `@feiyang666/dsh-mobile-remote >= 1.8.0` 支持**裸地址直达**。
- **v1.12.0 —— 适配 DeepSeek V4.1 Flash**：运行环境更新到官方最新版后，官方 Web UI 的模型选择器即出现 **`deepseek-flash`（DeepSeek-V4.1-Flash）** 与 `deepseek-v4-pro`（旧模型名在 0.1.5 及更早版本下仍可调用）；**用量与消耗插件升级到 ≥ 1.17.0** 后按官方 **2026-09-10 12:00（北京时间）** 生效的新峰谷价计费（空闲 缓存命中 0.02 / 输入 1 / 输出 4 元，高峰为其两倍；`deepseek-v4-pro` 不变，按每次调用发生时刻选择价格版本）。
- 同期的修复：修复「一键更新成功后桌面端没反应」（服务未运行时更新完成会直接以「极速启动」把服务拉起来）；启动就绪判定改为「真正可用」时刻（不再对着"已就绪"空等数秒）；新增数据中心栏目、插件市场、多镜像自动切换等。

## 核心特性

### 三种安装模式

| 模式 | 说明 | 适合场景 |
| --- | --- | --- |
| **极速启动** | 本地固定目录秒级启动，后台自动检查更新，一键更新 | 大多数用户，推荐 |
| **源码完整安装** | `git clone` + `pnpm build` | 开发者，想改源码/调试 |
| **本地修复** | 一键卸载、清理残留并重装 | 安装损坏、koffi 加载失败时修复 |

### 插件系统
- **插件管理**：一键安装 / 卸载推荐插件（`@feiyang666/dsh-usage-plugin`、`@feiyang666/dsh-vault`、`@feiyang666/dsh-mobile-remote`），或自定义填写 npm 包名安装
- **插件市场**：扫描 GitHub 上带 `dsh-plugin` 话题的公开仓库，支持搜索与分页，一键安装
- **数据中心**：侧边栏一站式展示全部插件的用量 / 余额 / 备份 / 远程设备数据（SSE 实时推送）

### 人性化体验
- **国内镜像加速**：依赖下载默认使用 npmmirror，可用环境变量 `DSH_NPM_REGISTRY` 切换任意镜像
- **中英文双语切换**：全界面支持简体中文 / English 一键切换
- 大百分比进度条 + 步骤指示器 + 可展开的命令行日志面板
- 界面主题切换：深色 / 浅色 / 跟随系统，与官方 WebUI 多端同步
- 系统托盘常驻、干净退出、在线检查更新

## 技术说明

- Electron 31，启用 `contextIsolation` 安全沙箱
- 打包：electron-builder + NSIS（Windows）
- 更新模块：`https` 下载 + SHA256 校验 + 系统通知
- 插件数据桥接：`plugin-bridge.js`（纯 Node 逻辑、无 Electron 依赖），经各插件 HTTP API / SSE 消费数据，自动携带 dsh 浏览器会话 cookie
- 认证适配：自动捕获 dsh 启动令牌 → 带 token 打开主界面 → 换取 30 天 cookie → 回裸地址；插件桥 cookie 注入

## 已知事项

- 极速启动首次需下载约数百 MB 依赖到本地固定目录（国内已默认走 npmmirror 镜像加速），之后启动秒级且完全离线
- 源码模式需已安装 git（pnpm 缺失时自动补齐）
- 新模型 `deepseek-flash` 需要 dsh 运行环境为官方最新版（v0.1.5+）；**dsh 0.1.7 起官方已把 `V4 Flash` 与 `V4 Flash Vision Exp` 从默认模型列表中移除**，模型选择器只保留新模型名（桌面端不参与模型清单，以 Web UI 实际显示为准）
- 用量统计要得到正确的 V4.1 价格，需把用量插件升级到 ≥ 1.17.0；未升级时新模型记录仍会被统计，但按 0 成本显示
- 移动端远程访问「裸地址直达」需插件 `@feiyang666/dsh-mobile-remote >= 1.8.0`；老版本插件仍显示带 token 地址、功能可用但体验不同
- 数据中心各区块依赖对应插件已安装 / 运行：插件未安装时显示「未安装」说明，不影响主流程
