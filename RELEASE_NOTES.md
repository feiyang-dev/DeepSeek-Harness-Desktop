# DeepSeek Harness 桌面版 v1.9.4

**DeepSeek Harness 官方 Web UI 的 Windows 桌面客户端** —— 自动检测环境、安装依赖、拉起服务，并支持在线检查更新，开箱即用。

## 安装方式

下载 **`DeepSeek Harness 桌面版-Setup-1.9.4.exe`**（NSIS 安装包），双击安装即可。

- 未安装 Node.js / git 时，客户端会自动下载官方安装包静默补齐

## 本版更新（v1.9.4）

> 本版本重点解决「安装/更新卡死 20 分钟」的问题：dsh 官方包拆成 150+ 个子包后，npm 解析依赖树存在性能缺陷（CPU 满转、十几分钟装不完）。本版**全面切换到 pnpm**，实测安装从"卡死"降到 **1 分多钟装完**；同时优化了镜像切换策略、设置页状态显示、清除本地运行环境、日志查看体验。

### 🚀 安装/更新性能大幅提升（全面切换到 pnpm）

- **极速启动安装/更新改用 pnpm**：实测 150+ 依赖 **1 分多钟装完**（此前 npm 需 15 分钟以上甚至卡死）
- **快速启动 / 本地修复改用 `pnpm dlx`**：等价于原 npx，依赖树解析快 10 倍以上
- **自动补齐 pnpm**：未安装时自动 `npm install -g pnpm`
- **修复装到旧版本的问题**：改用精确版本号安装（`npm view` 查询 dist-tags），确保装到官方最新版
- **修复 `--patch` 参数顺序**：移动端远程控制此前在特定版本下失效（`unknown option '--patch'`），现已修复

### 🔄 镜像切换优化

- **官方源改为最后兜底**：只有所有国内镜像都失败才用官方源
- **某个镜像缺包（404）自动换其他国内镜像**：不再"网易失败直接跳官方源"，而是先换 npmmirror / 腾讯 / 华为 / 淘宝 继续试

### 🔧 修复

- **设置页运行状态误显示"未运行"**：离开首页再进设置页，底部状态不再错误显示为未运行
- **清除本地运行环境卡"清除中"**：Windows 删除 pnpm 目录（junction 符号链接）改用 `cmd rmdir`，不再卡死转圈

### 📜 日志查看优化

- **首页新增「查看日志」按钮**：服务运行中一键进入日志区并定位
- **首次安装自动展开日志面板**、进度页日志不再被挤压（整页可滚动）
- **设置页日志区高度提升**，查看更全

### ⏹ 进度页可退出

- 安装/更新/清除进行中新增 **「取消操作」按钮**：确认后停止服务并退出应用，不再卡在进度页 100% 无法退出

## 核心特性

### 三种安装模式

| 模式 | 说明 | 适合场景 |
| --- | --- | --- |
| **极速启动** | 本地固定目录秒级启动，后台自动检查更新，一键更新 | 大多数用户，推荐 |
| **源码完整安装** | `git clone` + `pnpm build` | 开发者，想改源码/调试 |
| **本地修复** | 一键卸载、清理残留并重装 | 安装损坏、koffi 加载失败时修复 |

### 插件系统
- **插件管理**：一键安装 / 卸载推荐插件（`@feiyang666/dsh-usage-plugin`、`@feiyang666/dsh-vault`），或自定义填写 npm 包名安装
- **插件市场**：扫描 GitHub 上带 `dsh-plugin` 话题的公开仓库，支持搜索与分页，一键安装
- **兼容性自动修补**：安装 / 升级插件时自动修正违反 JSON Schema 规范的 `required` 字段，消除启动告警

### 人性化体验
- **国内镜像加速**：依赖下载默认使用 npmmirror，可用环境变量 `DSH_NPM_REGISTRY` 切换任意镜像
- **中英文双语切换**：全界面支持简体中文 / English 一键切换
- 大百分比进度条 + 步骤指示器 + 可展开的命令行日志面板
- 界面主题切换：深色 / 浅色 / 跟随系统，与官方 WebUI 多端同步
- 系统托盘常驻、干净退出、在线检查更新

## 技术说明

- Electron 31，启用 `contextIsolation` 安全沙箱
- 打包：electron-builder + NSIS（Windows）
- 更新模块：`https` 下载 + SHA256 校验 + 系统通知

## 已知事项

- 极速启动首次需下载约数百 MB 依赖到本地固定目录（国内已默认走 npmmirror 镜像加速），之后启动秒级且完全离线
- 源码模式需已安装 git（pnpm 缺失时自动补齐）
