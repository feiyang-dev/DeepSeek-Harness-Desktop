# DeepSeek Harness 桌面版 v1.1.0

**DeepSeek Harness 官方 Web UI 的 Windows 桌面客户端** —— 自动检测环境、安装依赖、拉起服务，并支持在线检查更新，开箱即用。

## 安装方式

下载 **`DeepSeek Harness 桌面版-Setup-1.1.0.exe`**（NSIS 安装包），双击安装即可。目标电脑需已安装 [Node.js](https://nodejs.org/)（缺失时客户端会引导下载）。

## 本版新增（v1.1.0）

### 在线更新功能
- **启动自动检测更新**：进入 APP 即连接更新服务检查，发现新版本立即弹出更新提示窗，可一键下载并安装
- 也可在「设置」中手动检查更新
- 下载过程显示实时进度，完成后校验安装包 SHA256 确保完整性
- 支持系统通知：发现新版本时弹窗提醒（可在设置中关闭）

### 环境自动修复
- **启动前自动预检**：检测 `~/.dsh/profiles/node_modules` 是否存在非符号链接的异常残留（dsh 契约要求全部为符号链接），异常时自动整体清理，dsh 下次启动自动重建符号链接，避免 "is not a symlink" 启动失败
- **服务启动失败自动修复**：若 dsh 服务因符号链接异常启动失败，自动清理并重启一次；仍失败才提示用户选择「本地修复」
- 启动失败时引导页提示选择「本地修复」，一键清理异常环境并重装

### 设置页面
- 引导页新增「设置」入口，进入独立设置页面
- 关于：应用版本号、更新日志、更新服务状态
- 通知：新版本通知开关（默认开启，可持久化保存）
- 检查更新：进入设置页自动检查，也可手动点击「立即检查」

### 引导页调整
- **移除倒计时自动进入**，三种启动模式完全由用户自行选择
- 保留：快速启动 / 源码完整安装 / 本地修复

## 核心特性

### 三种安装模式

| 模式 | 说明 | 适合场景 |
| --- | --- | --- |
| **快速启动** | 自动 `npm install -g @deepseek-ai/dsh` | 大多数用户，最快开始使用 |
| **源码完整安装** | `git clone` + `pnpm build` | 开发者，想改源码/调试 |
| **本地修复** | 一键卸载、清理残留并重装 | 安装损坏、koffi 加载失败时修复 |

### 人性化启动引导

- **国内镜像加速**：依赖下载默认使用 npmmirror（阿里云）镜像源，大幅提升下载速度；可用环境变量 `DSH_NPM_REGISTRY` 切换腾讯云/华为云等任意镜像
- 大百分比进度条 + **步骤指示器**：实时显示"步骤 x/n · 当前动作"（三种模式各有专属步骤清单，如修复模式：停止服务→卸载→清残留→重装→下载→解压→配置→启动）
- 阶段提示（检测环境 → 下载 → 安装 → 构建 → 启动），替代日志刷屏
- 实时细节：下载阶段显示"已处理 N 个依赖包"，服务启动显示"已等待 N 秒"
- 网络慢兜底：20 秒无进展即提示"仍在工作"，避免用户误以为卡死
- 可展开的命令行日志面板，出错时自动展开，随时查看真实输出
- 环境自动检测：Node.js / git / pnpm 缺失时给出对应安装引导

### 其他特性

- **无终端窗口**：所有子进程静默执行，不弹命令行窗口
- **服务自动拉起**：优先复用已有 3080 端口服务，否则自动启动 dsh web
- **系统托盘**：关闭窗口最小化到托盘，服务保持后台运行
- **干净退出**：退出时自动清理 dsh 子进程树，不留残留
- **内嵌官方 Web UI**：服务就绪后自动打开 `http://127.0.0.1:3080`

## 技术说明

- Electron 31，启用 `contextIsolation` 安全沙箱
- 主进程状态机：模式选择 → 环境检测 → 安装/构建 → 服务拉起 → 打开主界面
- 更新模块：`https` 下载 + SHA256 校验 + 系统通知（`update:status` 事件驱动 UI）
- 快速模式使用 `npm_config_ignore_scripts=true` 跳过 koffi 源码编译，避免依赖 CMake 失败
- 源码模式构建产物存于 `%APPDATA%/dsh-desktop/`，不污染工作区
- 打包：electron-builder + NSIS

## 已知事项

- 快速启动首次需下载约数百 MB 依赖
- 源码模式需已安装 git（pnpm 缺失时会自动安装）
