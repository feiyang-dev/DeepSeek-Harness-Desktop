# DeepSeek Harness 桌面版 v1.10.0

**DeepSeek Harness 官方 Web UI 的 Windows 桌面客户端** —— 自动检测环境、安装依赖、拉起服务，并支持在线检查更新，开箱即用。

## 安装方式

下载 **`DeepSeek Harness 桌面版-Setup-1.10.0.exe`**（NSIS 安装包），双击安装即可。

- 未安装 Node.js / git 时，客户端会自动下载官方安装包静默补齐

## 本版更新（v1.10.0）

> 本版新增「插件数据桥接」与「数据中心」栏目：首页运行状态控制台与侧边栏一站式展示已安装插件（用量插件 / 数据保险箱 / 移动端远程控制）的实时数据，并通过 SSE 实时推送，无需手动刷新。

### 🆕 插件数据桥接（新增）

- **首页运行状态控制台新增插件数据卡片**：服务运行期间，自动通过已安装插件暴露的 HTTP API 拉取并展示——用量插件（`dsh-usage-plugin`）的今日消耗 / 近 7 天消耗 / 余额、数据保险箱（`dsh-vault`）的备份份数与最近备份时间、移动端远程控制（`dsh-mobile-remote`）的在线设备数，每 60 秒自动刷新，服务停止时自动隐藏
- **插件保持完全独立可用**：上述能力全部来自插件自身（npm 包 + `dsh plugin` 一条命令即可独立使用，不依赖桌面端）；桌面端只做「桥接消费」，不重复实现任何插件逻辑。插件未安装 / 未运行 / 接口不可用时对应卡片自动隐藏，不影响主流程

### 🆕 数据中心栏目（新增）

- **侧边栏新增「数据中心」独立栏目**（与首页 / 插件管理 / 设置平级），一站式、分区块展示全部插件详细数据：
  - **用量统计**：今日 / 本周 / 本月 / 累计四周期汇总（成本 + 调用次数）、缓存命中率（含进度条）、近 30 天按天明细、按模型分布、按服务商分布、最近 50 条调用记录明细
  - **余额与凭据**：全部余额服务商（DeepSeek / SiliconFlow / DigitalOcean / AMD）的凭据配置状态、余额数值、余额明细（充值 / 赠送等）、查询时间、错误原因
  - **备份管理**：备份份数、最近备份时间、备份根目录、完整备份历史，以及「立即备份」按钮（调用 `dsh-vault` 执行手动备份）
  - **远程设备**：在线设备数、累计设备数、累计心跳、局域网访问地址、外网隧道状态、密码门禁状态、在线设备列表、服务运行时信息
- **SSE 实时推送，无需手动刷新**：服务运行期间，桌面端长连接订阅三个插件新增的 SSE 事件流（`/usage/api/events`、`/api/dsh-vault/events`、`/__dsh_remote/events`），插件在数据变化**瞬间**推送，桌面端收到即静默刷新对应数据；另有后台指标对比（20s）兜底；「立即刷新」按钮保留

## 核心特性

### 三种安装模式

| 模式 | 说明 | 适合场景 |
| --- | --- | --- |
| **极速启动** | 本地固定目录秒级启动，后台自动检查更新，一键更新 | 大多数用户，推荐 |
| **源码完整安装** | `git clone` + `pnpm build` | 开发者，想改源码/调试 |
| **本地修复** | 一键卸载、清理残留并重装 | 安装损坏、koffi 加载失败时修复 |

### 插件系统
- **插件管理**：一键安装 / 卸载推荐插件（`@feiyang666/dsh-usage-plugin`、`@feiyang666/dsh-vault`、`@feiyang666/dsh-mobile-remote`），或自定义填写 npm 包名安装
- **插件市场**：扫描 GitHub 上带 `dsh-plugin` 话题的公开仓库，支持搜索与分页，一键安装
- **兼容性自动修补**：安装 / 升级插件时自动修正违反 JSON Schema 规范的 `required` 字段，消除启动告警
- **数据中心**：侧边栏一站式展示全部插件的用量 / 余额 / 备份 / 远程设备数据（SSE 实时推送）

### 人性化体验
- **国内镜像加速**：依赖下载默认使用 npmmirror，可用环境变量 `DSH_NPM_REGISTRY` 切换任意镜像
- **中英文双语切换**：全界面支持简体中文 / English 一键切换
- 大百分比进度条 + 步骤指示器 + 可展开的命令行日志面板
- 界面主题切换：深色 / 浅色 / 跟随系统，与官方 WebUI 多端同步
- 系统托盘常驻、干净退出、在线检查更新

## 技术说明

- Electron 31，启用 `contextIsolation` 安全沙箱
- 打包：electron-builder + NSIS（Windows）
- 更新模块：`https` 下载 + SHA256 校验 + 系统通知
- 插件数据桥接：`plugin-bridge.js`（纯 Node 逻辑、无 Electron 依赖，可独立单测），经各插件 HTTP API / SSE 消费数据

## 已知事项

- 极速启动首次需下载约数百 MB 依赖到本地固定目录（国内已默认走 npmmirror 镜像加速），之后启动秒级且完全离线
- 源码模式需已安装 git（pnpm 缺失时自动补齐）
- 数据中心各区块依赖对应插件已安装 / 运行：插件未安装时显示「未安装」说明，不影响主流程
