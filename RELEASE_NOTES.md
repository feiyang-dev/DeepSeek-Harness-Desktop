# DeepSeek Harness 桌面版 v1.9.1

**DeepSeek Harness 官方 Web UI 的 Windows 桌面客户端** —— 自动检测环境、安装依赖、拉起服务，并支持在线检查更新，开箱即用。

## 安装方式

下载 **`DeepSeek Harness 桌面版-Setup-1.9.1.exe`**（NSIS 安装包），双击安装即可。

- 未安装 Node.js / git 时，客户端会自动下载官方安装包静默补齐

## 本版更新（v1.9.1）

### 极速启动模式（重大新增）

- 新增 **「极速启动」** 启动模式（推荐，取代原「快速启动」）：dsh 安装到本地固定目录 `%APPDATA%\dsh-desktop\dsh-local`，启动时直接用 Node 运行本地包入口，**不再每次走 npm exec / 解析依赖树**，二次以后启动秒级完成、完全离线可用
- **自动检查更新 + 一键更新**：极速启动后后台静默检查官方新版本，发现新版时首页运行状态栏显示「一键更新」横幅，点击自动「停止服务 → 重装本地环境到最新版 → 自动重启」，更新全程走国内镜像、失败自动切换
- 本地环境已就绪时跳过镜像测速，完全离线场景下无需网络探测即可秒级启动

### 启动崩溃自动修复（重大修复）

- **原生模块崩溃识别**：识别 Windows 原生崩溃码（0xC0000005 内存访问违规等），崩溃时自动清理损坏缓存并重启一次，仍失败给出针对性提示
- **npx 缓存损坏自动修复**：检测到缓存损坏自动清理重下，重下仍损坏自动升级 `npm cache verify` 深度校验（预算 2 次，防无限重启）
- **用户插件缺失自动摘除**：识别插件残留引用导致的 `Cannot find package` 并自动修复

### 查看日志（新增）

- 设置页新增 **「查看日志」** 区域：应用运行日志统一写入用户数据目录 `logs/dsh-desktop-YYYY-MM-DD.log`，按天分文件
- 支持刷新查看最近 2000 行、打开日志文件 / 日志目录，便于排查问题后直接反馈

### 用量插件包名变更（兼容升级）

- 用量与消耗插件推荐包名更名为 **`@feiyang666/dsh-usage-plugin`**（旧包名 `@feiyang666/deepseekharnessdesktop` 自动兼容识别）

### 界面修复（修复）

- 修复启动过程中离开首页再回来无法选择启动模式的问题
- 修复进度页阶段标签显示函数源码（如 `() => t('stageStart')`）的问题

## 核心特性

### 三种安装模式

| 模式 | 说明 | 适合场景 |
| --- | --- | --- |
| **极速启动** | 本地固定目录秒级启动，后台自动检查更新，一键更新 | 大多数用户，推荐 |
| **源码完整安装** | `git clone` + `pnpm build` | 开发者，想改源码/调试 |
| **本地修复** | 一键卸载、清理残留并重装 | 安装损坏、koffi 加载失败时修复 |

### 插件系统
- **插件管理**：一键安装 / 卸载推荐插件（`@feiyang666/dsh-usage-plugin`、`@feiyang666/dsh-vault`），或自定义填写 npm 包名安装
- **插件市场**：扫描 GitHub 上带 `dsh-plugin` 话题的公开仓库，支持搜索与分页，一键安装
- **兼容性自动修补**：安装 / 升级插件时自动修正违反 JSON Schema 规范的 `required` 字段，消除启动告警

### 人性化体验
- **国内镜像加速**：依赖下载默认使用 npmmirror，可用环境变量 `DSH_NPM_REGISTRY` 切换任意镜像
- **中英文双语切换**：全界面支持简体中文 / English 一键切换
- 大百分比进度条 + 步骤指示器 + 可展开的命令行日志面板
- 界面主题切换：深色 / 浅色 / 跟随系统，与官方 WebUI 多端同步
- 系统托盘常驻、干净退出、在线检查更新

## 技术说明

- Electron 31，启用 `contextIsolation` 安全沙箱
- 打包：electron-builder + NSIS（Windows）
- 更新模块：`https` 下载 + SHA256 校验 + 系统通知

## 已知事项

- 极速启动首次需下载约数百 MB 依赖到本地固定目录（国内已默认走 npmmirror 镜像加速），之后启动秒级且完全离线
- 源码模式需已安装 git（pnpm 缺失时自动补齐）
