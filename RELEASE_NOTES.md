# DeepSeek Harness 桌面版 v1.9.3

**DeepSeek Harness 官方 Web UI 的 Windows 桌面客户端** —— 自动检测环境、安装依赖、拉起服务，并支持在线检查更新，开箱即用。

## 安装方式

下载 **`DeepSeek Harness 桌面版-Setup-1.9.3.exe`**（NSIS 安装包），双击安装即可。

- 未安装 Node.js / git 时，客户端会自动下载官方安装包静默补齐

## 本版更新（v1.9.3）

> 本版本在 v1.9.1 基础上继续优化：重点重构了进度显示系统（让进度条更准确、所有模式必达 100%）、极速启动二次提速、新增清除本地运行环境、优化退出确认与命令行日志。

### 关于「快速启动」→「极速启动」的合并说明

本系列版本（v1.9.1 起）已将原 **「快速启动」合并为「极速启动」**：原快速启动每次启动走 `npm exec`（npx），因 dsh 官方包拆成 150+ 个子包，每次都要逐个向 registry 解析依赖树、校验并解压，缓存命中也要 100~200 秒。极速启动把「安装」与「启动」彻底分离——首次把 dsh 及全部依赖安装到本地固定目录，之后启动直接用 Node 运行本地入口，**秒级完成、完全离线**；同时保留「后台自动检查官方新版 + 一键更新」，官方发布新版后点一次即可升级。首页的快速入口现为「极速启动」（原快速启动卡片已移除，内部实现保留）。

### 进度显示系统重构（优化）

- **进度单调递增**：进度条只增不减，百分比与阶段文字不再脱节（修复"正在安装依赖却显示 60%"、"到不了 100%"的问题）
- **所有模式必达 100%**：服务就绪后统一置 100%
- **安装阶段"慢进"**：首次下载数百 MB 依赖时进度持续走动，不再像"卡死"
- **首次安装醒目提示**：首次装依赖 / Node.js 安装 / 一键更新时，进度页显示「⏳ 首次安装运行环境，通常需 3~10 分钟，请耐心等待」提示卡

### 极速启动提速（优化）

- 环境定位缓存、本地就绪跳过 npm、就绪轮询 300ms，二次启动更快
- 首页运行状态「已运行」秒数改为**每秒实时更新**

### 清除本地运行环境（新增）

- 设置页「运行环境（dsh）」新增**本地运行环境位置展示与清除**：显示安装目录完整路径，一键清除（停止服务 → 删除目录），清除后下次选「极速启动」会重新联网安装

### 退出确认优化（优化）

- WebUI 关闭时弹自定义/原生确认框，提供**「退出 Web 界面」**（保留后台服务，可随时在引导台重新打开）与**「退出 APP」**（清理服务后完全退出）两个选择

### 命令行日志优化（优化）

- 日志面板新增 **「隐藏依赖下载进度」**（默认开启，过滤 `npm http fetch` 刷屏）与 **「暂停自动滚动」** 开关，便于查看报错

### 界面修复（修复）

- 修复启动过程中离开首页再回来无法选择启动模式的问题
- 修复启动完成后切到设置/插件管理再回首页卡在 100% 的问题
- 修复进度页阶段标签显示函数源码（如 `() => t('stageStart')`）的问题

## 核心特性

### 三种安装模式

| 模式 | 说明 | 适合场景 |
| --- | --- | --- |
| **极速启动** | 本地固定目录秒级启动，后台自动检查更新，一键更新 | 大多数用户，推荐 |
| **源码完整安装** | `git clone` + `pnpm build` | 开发者，想改源码/调试 |
| **本地修复** | 一键卸载、清理残留并重装 | 安装损坏、koffi 加载失败时修复 |

### 插件系统
- **插件管理**：一键安装 / 卸载推荐插件（`@feiyang666/dsh-usage-plugin`、`@feiyang666/dsh-vault`），或自定义填写 npm 包名安装
- **插件市场**：扫描 GitHub 上带 `dsh-plugin` 话题的公开仓库，支持搜索与分页，一键安装
- **兼容性自动修补**：安装 / 升级插件时自动修正违反 JSON Schema 规范的 `required` 字段，消除启动告警

### 人性化体验
- **国内镜像加速**：依赖下载默认使用 npmmirror，可用环境变量 `DSH_NPM_REGISTRY` 切换任意镜像
- **中英文双语切换**：全界面支持简体中文 / English 一键切换
- 大百分比进度条 + 步骤指示器 + 可展开的命令行日志面板
- 界面主题切换：深色 / 浅色 / 跟随系统，与官方 WebUI 多端同步
- 系统托盘常驻、干净退出、在线检查更新

## 技术说明

- Electron 31，启用 `contextIsolation` 安全沙箱
- 打包：electron-builder + NSIS（Windows）
- 更新模块：`https` 下载 + SHA256 校验 + 系统通知

## 已知事项

- 极速启动首次需下载约数百 MB 依赖到本地固定目录（国内已默认走 npmmirror 镜像加速），之后启动秒级且完全离线
- 源码模式需已安装 git（pnpm 缺失时自动补齐）
